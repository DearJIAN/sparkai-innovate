# 火花智创 SparkAI Innovate 在线测评功能设计思路

> 文件用途：交给 AICoder / Trae CN AI Coder 执行开发。  
> 目标：在现有「火花智创 SparkAI Innovate」项目中新增一个类似参考截图的「在线测评」入口、测评中心、答题页、结果页。  
> 重要约束：只把用户指定的测评做成真实可填写、可提交、可生成结果；其他测评只做入口，进入后显示「建设中」。

---

## 1. 背景与目标

当前项目首页已经有顶部导航栏和功能入口卡片，例如「竞赛报名」「我的赛事」「项目工作室」「团队管理」「材料中心」「AI项目助手」「训练营」「在线课程」「产业命题」「证书成果」等。

本次新增功能是「在线测评」，参考用户提供的图 3、4、5、6、7 的页面形态：

1. 顶部导航栏新增「在线测评」。
2. 首页功能卡片区新增一个「在线测评」卡片。
3. 新增在线测评中心页面，左侧是分类导航，右侧是测评卡片列表。
4. 测评卡片支持点击进入。
5. 指定测评进入真实答题页，题目可选择、可切题、有进度、有动画。
6. 提交后进入测评结果页，展示分数、完成率、评价、维度分析和改进建议。
7. 非真实测评只显示入口，进入后显示「建设中」。

---

## 2. 参考视觉与交互总结

### 2.1 测评中心参考

参考页面大致结构：

- 顶部导航栏固定在页面顶部。
- 页面顶部有蓝色渐变 Banner，标题类似「在线测评中心」。
- 主内容区域是左右布局：
  - 左侧分类侧栏。
  - 右侧测评列表。
- 测评列表使用卡片式入口。
- 卡片上半部分是测评封面图或渐变图。
- 卡片下半部分是标题、简介、完成时间、浏览量/热度、按钮。
- 按钮文案类似「开始测评」。

### 2.2 答题页参考

答题页大致结构：

- 顶部有简洁 header，左侧是「在线测评」，中间是测评名称、进度、倒计时。
- 页面中间左侧是题目卡片。
- 右侧是测评信息面板：
  - 测评名称
  - 测评时长
  - 试卷总数
  - 总题数
  - 已答题数
  - 答题进度
  - 题目导航
- 底部有上一题、下一题、提交按钮。
- 题目卡片里的选项可点击，选中后需要有状态变化。

### 2.3 结果页参考

结果页大致结构：

- 顶部标题「测评结果分析」。
- 综合评估卡片：
  - 圆环分数
  - 测评项目
  - 总题数 / 得分
  - 完成率 / 得分率
- 评价与建议区域：
  - 综合评价
  - 改进建议
  - 维度分析
  - 行动建议
- 页面整体清爽、留白充足、蓝色科技感。

---

## 3. 功能范围

### 3.1 真实实现的测评

本次只真实实现 5 个测评，每个测评 10 道题：

#### 创业测评：4 个全部真实

1. 创业精神测评
2. 创业性格测评
3. 创业兴趣测评
4. 创业能力测评

#### 能力测评：只做 1 个真实

5. 团队合作能力测评

### 3.2 只做入口 + 建设中的测评

以下测评只做卡片入口，点击进入后显示「建设中」页面：

#### 就业测评

1. 职业气质测评
2. 职业价值观测评
3. 职业兴趣测评
4. 职业性格测评

#### 能力测评

1. 自主学习能力测评
2. 沟通交际能力测评
3. 情绪控制能力测评

---

## 4. 信息架构设计

### 4.1 页面结构

建议新增以下页面：

```text
frontend/src/views/portal/
├── OnlineAssessment.vue              # 在线测评中心页
├── AssessmentDetail.vue              # 真实测评答题页
├── AssessmentResult.vue              # 测评结果页
└── AssessmentComingSoon.vue          # 建设中页面
```

建议新增数据文件：

```text
frontend/src/data/
└── assessmentBank.js                 # 测评分类、卡片、题库、评分规则
```

如果项目希望 API 分层更统一，可以新增：

```text
frontend/src/api/
└── assessment.js                     # 暂时封装本地数据读取；未来可替换为后端接口
```

本期可以先做纯前端静态题库和本地结果计算，不强制新增后端表。  
如果后续要保存用户历史测评记录，再加后端 API 和数据库表。

---

## 5. 路由设计

在 `frontend/src/router/index.js` 中新增路由。

建议路径：

```js
{
  path: '/assessment',
  name: 'OnlineAssessment',
  component: () => import('@/views/portal/OnlineAssessment.vue'),
  meta: {
    title: '在线测评',
    platformPage: true
  }
},
{
  path: '/assessment/:id',
  name: 'AssessmentDetail',
  component: () => import('@/views/portal/AssessmentDetail.vue'),
  meta: {
    title: '开始测评',
    platformPage: true
  }
},
{
  path: '/assessment/:id/result',
  name: 'AssessmentResult',
  component: () => import('@/views/portal/AssessmentResult.vue'),
  meta: {
    title: '测评结果',
    platformPage: true
  }
},
{
  path: '/assessment/:id/coming-soon',
  name: 'AssessmentComingSoon',
  component: () => import('@/views/portal/AssessmentComingSoon.vue'),
  meta: {
    title: '建设中',
    platformPage: true
  }
}
```

### 5.1 鉴权建议

本期建议「在线测评中心」允许未登录访问，符合平台首页的开放导流属性。

答题页也可以允许未登录访问，结果用 `sessionStorage` 暂存。  
如果当前项目路由守卫默认要求登录，则可以这样处理：

- `/assessment` 不要求登录。
- `/assessment/:id` 不要求登录。
- `/assessment/:id/result` 不要求登录，但没有结果数据时返回测评详情页。
- 后续如果要保存历史记录，再在提交结果时提示用户登录。

---

## 6. 首页改造

### 6.1 顶部导航新增

在当前首页顶部导航里新增：

```text
在线测评
```

建议放在「在线课程」附近，完整顺序可参考：

```text
首页 / 校外竞赛 / 校内竞赛 / 训练营 / 课程 / 在线测评 / 产业命题 / 我的项目 / 证书
```

或者如果项目已有「在线课程」文案，则建议：

```text
首页 / 校外竞赛 / 校内竞赛 / 训练营 / 课程 / 产业命题 / 在线测评 / 我的项目 / 证书
```

点击后跳转：

```text
/assessment
```

### 6.2 首页功能卡片新增

在首页「功能入口」卡片区新增：

```text
在线测评
获取创业能力、团队协作等能力画像
```

建议图标：

- `TrendCharts`
- `DataAnalysis`
- `EditPen`
- `Memo`
- `Histogram`

如果项目里使用 Element Plus Icons，优先使用已有图标，避免额外依赖。  
如果图标风格不统一，再调用 `better-icons` 获取统一 SVG 图标。

卡片色彩建议：

```text
主色：#2563eb / #3b82f6
辅助渐变：#06b6d4 → #2563eb
卡片顶部描边：蓝紫渐变
```

卡片点击跳转：

```text
/assessment
```

---

## 7. 在线测评中心页面设计

### 7.1 页面布局

`OnlineAssessment.vue` 推荐结构：

```text
OnlineAssessment.vue
├── 顶部 Banner
│   ├── 标题：在线测评中心
│   ├── 副标题：探索创业潜能、团队协作与职业发展画像
│   └── 统计：5 个真实测评 / 7 个即将开放 / 50 道真实题目
│
├── 主内容 assessment-layout
│   ├── 左侧分类 sidebar
│   │   ├── 全部分类
│   │   ├── 创业测评
│   │   │   ├── 创业精神测评
│   │   │   ├── 创业性格测评
│   │   │   ├── 创业兴趣测评
│   │   │   └── 创业能力测评
│   │   ├── 就业测评
│   │   │   ├── 职业气质测评
│   │   │   ├── 职业价值观测评
│   │   │   ├── 职业兴趣测评
│   │   │   └── 职业性格测评
│   │   └── 能力测评
│   │       ├── 团队合作能力测评
│   │       ├── 自主学习能力测评
│   │       ├── 沟通交际能力测评
│   │       └── 情绪控制能力测评
│   │
│   └── 右侧内容
│       ├── 标题：全部测评 / 当前分类
│       ├── 搜索框
│       ├── 卡片网格
│       └── 空状态
```

### 7.2 分类数据

建议在 `assessmentBank.js` 中维护：

```js
export const assessmentCategories = [
  {
    key: 'all',
    name: '全部分类',
    icon: 'Menu'
  },
  {
    key: 'entrepreneurship',
    name: '创业测评',
    children: [
      'entrepreneurial-spirit',
      'entrepreneurial-personality',
      'entrepreneurial-interest',
      'entrepreneurial-ability'
    ]
  },
  {
    key: 'employment',
    name: '就业测评',
    children: [
      'career-temperament',
      'career-values',
      'career-interest',
      'career-personality'
    ]
  },
  {
    key: 'ability',
    name: '能力测评',
    children: [
      'teamwork-ability',
      'self-learning-ability',
      'communication-ability',
      'emotion-control-ability'
    ]
  }
]
```

### 7.3 卡片字段

每张测评卡建议字段：

```js
{
  id: 'entrepreneurial-spirit',
  title: '创业精神测评',
  category: 'entrepreneurship',
  categoryName: '创业测评',
  description: '评估创业主动性、机会意识、坚韧程度与风险承担倾向。',
  duration: 20,
  questionCount: 10,
  real: true,
  heat: 86,
  coverType: 'gradient',
  gradient: ['#14b8a6', '#2563eb'],
  tags: ['创业测评', '真实可测']
}
```

`real` 字段决定点击行为：

```js
function handleStart(item) {
  if (item.real) {
    router.push(`/assessment/${item.id}`)
  } else {
    router.push(`/assessment/${item.id}/coming-soon`)
  }
}
```

### 7.4 卡片视觉建议

卡片样式：

```text
宽度：两列布局，移动端一列
圆角：16px
阴影：0 10px 30px rgba(15, 23, 42, 0.08)
Hover：上浮 4px，阴影增强
封面高度：150px - 180px
按钮：右下角「开始测评」/「即将开放」
```

真实测评卡片显示：

```text
蓝色标签：真实测评
按钮：开始测评 →
```

建设中卡片显示：

```text
灰色或橙色标签：建设中
按钮：查看详情 →
```

---

## 8. 答题页设计

### 8.1 基础流程

真实测评进入 `AssessmentDetail.vue`：

1. 根据路由 `id` 查找题库。
2. 如果不存在，跳转 404 或返回测评中心。
3. 如果存在但 `real === false`，跳转建设中页。
4. 初始化答题状态：
   - 当前题号 `currentIndex = 0`
   - 答案对象 `answers = {}`
   - 开始时间 `startTime`
   - 倒计时 `remainingSeconds`
5. 用户选择选项。
6. 点击下一题。
7. 最后一题显示「提交测评」。
8. 提交时校验是否全部答完。
9. 计算结果并写入 `sessionStorage`。
10. 跳转结果页。

### 8.2 页面结构

```text
AssessmentDetail.vue
├── 顶部固定 header
│   ├── Logo / 在线测评
│   ├── 测评名称
│   ├── 题目进度 1/10
│   ├── 进度条
│   └── 倒计时
│
├── 页面主体
│   ├── 左侧题目卡片
│   │   ├── 标签：第 N 题
│   │   ├── 题目内容
│   │   └── 选项列表
│   └── 右侧信息卡
│       ├── 测评信息
│       ├── 答题进度
│       ├── 题目导航
│       └── 图例：未答 / 已答 / 当前
│
└── 底部操作栏
    ├── 上一题
    ├── 下一题 / 提交测评
    └── 返回测评中心
```

### 8.3 动画效果要求

必须做轻量动画，不要过度炫技。

建议：

1. 题目切换动画：
   - 使用 Vue `<Transition name="question-fade" mode="out-in">`
   - 当前题切换时淡入 + 轻微上移。
2. 选项选中动画：
   - 点击后边框变蓝。
   - 背景变浅蓝。
   - 左侧 radio 圆点填充。
   - 可加 `transform: translateX(4px)`。
3. 进度条动画：
   - `width` 使用 `transition: width .35s ease`。
4. 题号导航动画：
   - 当前题蓝色描边。
   - 已答题绿色背景。
5. 提交后结果页：
   - 分数使用 count-up 动画。
   - 圆环进度使用 CSS `conic-gradient` 或 ECharts 环图。
   - 结果卡片淡入。

---

## 9. 测评结果页设计

### 9.1 数据来源

结果页 `AssessmentResult.vue` 从 `sessionStorage` 读取：

```js
const resultKey = `assessment_result_${assessmentId}`
const result = JSON.parse(sessionStorage.getItem(resultKey) || 'null')
```

如果没有结果：

```text
提示：暂无测评结果，请先完成测评
按钮：开始测评
```

### 9.2 结果页结构

```text
AssessmentResult.vue
├── 顶部说明
│   ├── 标题：测评结果分析
│   └── 副标题：基于你的答题表现生成能力画像
│
├── 综合评估卡片
│   ├── 圆环分数
│   ├── 等级标签
│   ├── 测评项目
│   ├── 总题数 / 得分
│   └── 完成率 / 得分率
│
├── 评价与建议
│   ├── 综合评价
│   ├── 改进建议
│   ├── 优势表现
│   └── 风险提醒
│
├── 维度分析
│   ├── 维度条形图
│   └── 可选 ECharts 雷达图
│
└── 操作按钮
    ├── 重新测评
    ├── 返回测评中心
    └── 返回首页
```

### 9.3 结果等级规则

每个测评 10 题，每题最高 5 分，总分 50 分。

```text
45 - 50：优势突出
38 - 44：表现良好
30 - 37：中等发展
20 - 29：有待提升
10 - 19：需要重点培养
```

结果对象建议：

```js
{
  assessmentId,
  title,
  totalScore,
  maxScore: 50,
  percent,
  level,
  levelText,
  summary,
  suggestions,
  strengths,
  risks,
  dimensions: [
    {
      name: '主动性',
      score: 8,
      maxScore: 10,
      percent: 80
    }
  ],
  answers,
  completedAt,
  usedSeconds
}
```

---

## 10. 题库与评分设计

### 10.1 通用选项

为了降低开发复杂度，5 个真实测评统一使用 5 点量表：

```text
A. 非常符合  5分
B. 比较符合  4分
C. 一般      3分
D. 不太符合  2分
E. 完全不符合 1分
```

如需反向题，可在题目中设置 `reverse: true`，计算时反向计分：

```js
function getScore(optionScore, reverse) {
  return reverse ? 6 - optionScore : optionScore
}
```

### 10.2 题目数据结构

```js
{
  id: 1,
  text: '我经常主动观察身边的问题，并思考是否存在新的解决方案。',
  dimension: '机会意识',
  reverse: false
}
```

渲染时自动使用通用选项。  
这样题库更干净，也方便后续扩展。

---

## 11. 真实题库内容

下面题库可以直接写入 `frontend/src/data/assessmentBank.js`。

### 11.1 创业精神测评：10 题

测评 ID：

```text
entrepreneurial-spirit
```

维度建议：

```text
机会意识 / 主动性 / 坚韧性 / 风险承担 / 创新驱动
```

题目：

1. 我经常主动观察身边的问题，并思考是否存在新的解决方案。  
   维度：机会意识

2. 当我发现一个有价值的想法时，会愿意进一步调研它是否可行。  
   维度：主动性

3. 面对不确定的任务，我不会马上退缩，而是会先尝试找到突破口。  
   维度：坚韧性

4. 我愿意为了一个有潜力的项目投入额外时间进行验证。  
   维度：主动性

5. 当别人否定我的想法时，我会重新审视证据，而不是直接放弃。  
   维度：坚韧性

6. 我能接受创业项目早期存在试错和失败的可能。  
   维度：风险承担

7. 我喜欢把不同领域的知识组合起来，形成新的创意。  
   维度：创新驱动

8. 我会关注用户、市场或校园场景中的真实痛点。  
   维度：机会意识

9. 如果一个项目短期内看不到成果，我仍能保持一定耐心继续推进。  
   维度：坚韧性

10. 我愿意承担合理风险，去争取更有价值的成长机会。  
    维度：风险承担

### 11.2 创业性格测评：10 题

测评 ID：

```text
entrepreneurial-personality
```

维度建议：

```text
开放性 / 责任感 / 外向协作 / 情绪稳定 / 决策倾向
```

题目：

1. 我愿意尝试新的方法，即使它和过去的做法不同。  
   维度：开放性

2. 我通常会按计划推进任务，并尽量按时完成。  
   维度：责任感

3. 在团队讨论中，我愿意表达自己的观点并听取他人意见。  
   维度：外向协作

4. 遇到压力时，我通常能保持基本冷静。  
   维度：情绪稳定

5. 面对多个选择时，我会尽量收集信息后再做判断。  
   维度：决策倾向

6. 我能接受别人提出和我不同的观点。  
   维度：开放性

7. 我不太愿意在团队中承担明确责任。  
   维度：责任感  
   反向计分：是

8. 当项目遇到阻力时，我容易被情绪影响，难以继续推进。  
   维度：情绪稳定  
   反向计分：是

9. 我愿意和陌生人沟通，以获取资源、建议或反馈。  
   维度：外向协作

10. 我倾向于在行动前判断风险与收益，而不是完全凭冲动。  
    维度：决策倾向

### 11.3 创业兴趣测评：10 题

测评 ID：

```text
entrepreneurial-interest
```

维度建议：

```text
市场兴趣 / 产品兴趣 / 技术兴趣 / 组织兴趣 / 社会价值
```

题目：

1. 我对发现用户需求和市场机会很感兴趣。  
   维度：市场兴趣

2. 我喜欢思考一个产品怎样设计才更好用。  
   维度：产品兴趣

3. 我愿意了解新技术如何转化为实际应用。  
   维度：技术兴趣

4. 我喜欢组织同学或伙伴一起完成一个目标。  
   维度：组织兴趣

5. 我希望自己的项目能解决真实社会问题。  
   维度：社会价值

6. 我会主动关注创新创业比赛、商业案例或创业故事。  
   维度：市场兴趣

7. 我喜欢把一个想法画成流程图、原型图或功能清单。  
   维度：产品兴趣

8. 我愿意学习工具、技术或方法来提升项目实现能力。  
   维度：技术兴趣

9. 我对协调团队分工、推进计划和整合资源有兴趣。  
   维度：组织兴趣

10. 如果项目能产生积极影响，我会更愿意持续投入。  
    维度：社会价值

### 11.4 创业能力测评：10 题

测评 ID：

```text
entrepreneurial-ability
```

维度建议：

```text
问题定义 / 方案设计 / 资源整合 / 执行推进 / 表达路演
```

题目：

1. 我能把一个模糊想法拆解成清晰的问题和目标。  
   维度：问题定义

2. 我能根据用户需求提出多个可能的解决方案。  
   维度：方案设计

3. 我知道如何寻找资料、导师、同伴或工具来支持项目推进。  
   维度：资源整合

4. 我能把项目任务拆成阶段计划，并持续跟进进度。  
   维度：执行推进

5. 我能用简洁语言说明项目解决了什么问题。  
   维度：表达路演

6. 我会通过访谈、问卷或竞品分析验证自己的判断。  
   维度：问题定义

7. 我能根据反馈调整产品功能或商业模式。  
   维度：方案设计

8. 当团队资源不足时，我会尝试寻找替代方案。  
   维度：资源整合

9. 我不会只停留在想法层面，而会尽量做出可展示的成果。  
   维度：执行推进

10. 我能围绕痛点、方案、优势和价值讲清楚一个项目。  
    维度：表达路演

### 11.5 团队合作能力测评：10 题

测评 ID：

```text
teamwork-ability
```

维度建议：

```text
协作意识 / 沟通反馈 / 冲突处理 / 责任承担 / 共创能力
```

题目：

1. 团队分工后，我会主动确认自己的任务边界和交付时间。  
   维度：责任承担

2. 当队友遇到困难时，我愿意在能力范围内提供帮助。  
   维度：协作意识

3. 我能清楚表达自己的想法，也愿意听完别人的观点。  
   维度：沟通反馈

4. 团队出现分歧时，我会尽量回到目标和事实本身讨论。  
   维度：冲突处理

5. 我愿意和队友共同打磨方案，而不是只坚持自己的想法。  
   维度：共创能力

6. 如果我无法按时完成任务，会提前告知团队并说明原因。  
   维度：责任承担

7. 我不喜欢团队讨论，通常更愿意独自完成所有事情。  
   维度：协作意识  
   反向计分：是

8. 当别人指出我的问题时，我通常会先理解对方的依据。  
   维度：沟通反馈

9. 团队冲突升级时，我容易情绪化，难以继续沟通。  
   维度：冲突处理  
   反向计分：是

10. 我能把不同成员的想法整合成更完整的方案。  
    维度：共创能力

---

## 12. 数据文件建议写法

`assessmentBank.js` 建议导出：

```js
export const commonOptions = [
  { label: 'A', text: '非常符合', score: 5 },
  { label: 'B', text: '比较符合', score: 4 },
  { label: 'C', text: '一般', score: 3 },
  { label: 'D', text: '不太符合', score: 2 },
  { label: 'E', text: '完全不符合', score: 1 }
]

export const assessmentCategories = [
  // 分类数据
]

export const assessmentList = [
  // 卡片列表 + 题库
]

export function getAssessmentById(id) {
  return assessmentList.find(item => item.id === id)
}

export function getAssessmentsByCategory(category) {
  if (!category || category === 'all') return assessmentList
  return assessmentList.filter(item => item.category === category)
}

export function calculateAssessmentResult(assessment, answers) {
  // 统一评分逻辑
}
```

### 12.1 评分函数逻辑

```js
export function calculateAssessmentResult(assessment, answers) {
  const questions = assessment.questions || []
  let totalScore = 0
  const dimensionMap = {}

  questions.forEach((question) => {
    const rawScore = Number(answers[question.id] || 0)
    const finalScore = question.reverse ? 6 - rawScore : rawScore
    totalScore += finalScore

    if (!dimensionMap[question.dimension]) {
      dimensionMap[question.dimension] = {
        name: question.dimension,
        score: 0,
        maxScore: 0
      }
    }

    dimensionMap[question.dimension].score += finalScore
    dimensionMap[question.dimension].maxScore += 5
  })

  const maxScore = questions.length * 5
  const percent = maxScore ? Math.round((totalScore / maxScore) * 100) : 0

  const dimensions = Object.values(dimensionMap).map(item => ({
    ...item,
    percent: item.maxScore ? Math.round((item.score / item.maxScore) * 100) : 0
  }))

  return {
    assessmentId: assessment.id,
    title: assessment.title,
    totalScore,
    maxScore,
    percent,
    questionCount: questions.length,
    completedRate: 100,
    level: getLevel(totalScore),
    levelText: getLevelText(totalScore),
    summary: getSummary(assessment.id, totalScore, dimensions),
    suggestions: getSuggestions(assessment.id, totalScore, dimensions),
    dimensions,
    completedAt: new Date().toISOString()
  }
}
```

---

## 13. 建设中页面设计

`AssessmentComingSoon.vue` 用于非真实测评。

页面内容：

```text
标题：该测评正在建设中
副标题：我们正在完善题库、评分模型和结果分析能力，敬请期待。
卡片信息：显示测评名称、所属分类、预计开放方向
按钮：
- 返回在线测评中心
- 查看已开放测评
```

视觉建议：

- 中心卡片。
- 插画或图标：施工中 / 火花 / 文档。
- 背景浅蓝灰。
- 可加入小标签「即将开放」。

---

## 14. 响应式设计

### 14.1 桌面端

```text
页面最大宽度：1200px - 1280px
测评中心：左侧 260px，右侧自适应
卡片网格：2 列
答题页：左侧题目区 + 右侧信息卡
```

### 14.2 平板端

```text
测评中心：左侧分类可变成顶部横向 tab
卡片网格：2 列或 1 列
答题页：右侧信息卡下移
```

### 14.3 移动端

```text
顶部导航适配已有项目逻辑
测评中心：分类横向滚动
卡片一列
答题页：题目卡片优先，题目导航折叠
底部操作栏固定
```

---

## 15. 与现有设计系统的统一

当前项目整体是蓝色、青色、科技感、平台化风格，并有数字雨背景和 Live2D 火花虚拟形象。

在线测评功能建议：

1. 不使用过重暗黑风。
2. 采用亮色清爽风格。
3. 使用蓝色主色和青色辅助色。
4. 卡片保持圆角、轻阴影、顶部渐变封面。
5. 结果页可使用 ECharts，但避免页面过于复杂。
6. 动效要轻，控制在 hover、fade、progress、count-up 级别。

推荐 CSS 变量：

```css
:root {
  --assessment-primary: #2563eb;
  --assessment-primary-light: #dbeafe;
  --assessment-cyan: #06b6d4;
  --assessment-success: #10b981;
  --assessment-warning: #f59e0b;
  --assessment-danger: #ef4444;
  --assessment-text: #0f172a;
  --assessment-subtext: #64748b;
  --assessment-bg: #f8fafc;
  --assessment-card: #ffffff;
}
```

---

## 16. 开发步骤建议

### 阶段一：数据层

1. 新建 `frontend/src/data/assessmentBank.js`。
2. 写入分类数据。
3. 写入 12 个测评卡片。
4. 其中 5 个 `real: true` 且包含 10 道题。
5. 其他 7 个 `real: false` 且不需要题库。
6. 写入评分函数。

验收标准：

- 能通过 ID 找到测评。
- 能通过分类筛选测评。
- 能对 10 题答案计算出总分、维度分和等级。

### 阶段二：路由层

1. 在 `router/index.js` 新增 `/assessment`。
2. 新增 `/assessment/:id`。
3. 新增 `/assessment/:id/result`。
4. 新增 `/assessment/:id/coming-soon`。
5. 确认路由 meta 与平台页布局兼容。

验收标准：

- 访问 `/assessment` 能打开测评中心。
- 访问真实测评 ID 能打开答题页。
- 访问非真实测评 ID 能打开建设中页。

### 阶段三：首页入口

1. 修改顶部导航。
2. 修改首页功能入口卡片。
3. 新增「在线测评」卡片。
4. 点击跳转 `/assessment`。

验收标准：

- 顶部导航显示在线测评。
- 功能卡片显示在线测评。
- 点击均能跳转。

### 阶段四：测评中心页

1. 实现 Banner。
2. 实现左侧分类。
3. 实现搜索框。
4. 实现卡片网格。
5. 实现真实测评和建设中测评不同状态。
6. 实现分类筛选和搜索筛选。

验收标准：

- 创业测评能显示 4 个真实测评。
- 就业测评能显示 4 个建设中测评。
- 能力测评能显示 1 个真实测评 + 3 个建设中测评。
- 搜索标题关键词能过滤卡片。

### 阶段五：答题页

1. 读取测评题库。
2. 渲染当前题。
3. 实现选项选择。
4. 实现上一题 / 下一题。
5. 实现题号导航。
6. 实现进度条。
7. 实现倒计时。
8. 实现提交校验。
9. 实现轻量动画。
10. 提交后写入 sessionStorage 并跳转结果页。

验收标准：

- 真实测评能完成 10 题答题。
- 未答完时提交会提示。
- 题目导航状态正确。
- 进度显示正确。
- 提交后能生成结果。

### 阶段六：结果页

1. 从 sessionStorage 读取结果。
2. 展示综合评分。
3. 展示等级。
4. 展示综合评价。
5. 展示改进建议。
6. 展示维度分析。
7. 实现重新测评和返回按钮。
8. 实现分数动画。

验收标准：

- 每个真实测评都有结果页。
- 分数和维度分计算正确。
- 没有结果时有空状态，不报错。

### 阶段七：建设中页

1. 根据 ID 展示测评名称。
2. 显示建设中说明。
3. 提供返回测评中心按钮。
4. 提供查看已开放测评按钮。

验收标准：

- 所有非真实测评点击后进入建设中页。
- 页面风格和平台统一。

---

## 17. 质量要求

### 17.1 代码质量

1. 不写占位 TODO。
2. 不写伪代码替代真实实现。
3. 组件内状态命名清晰。
4. 题库和评分逻辑放到数据文件，不要全部塞进页面组件。
5. 答题页要处理异常 ID、非真实测评、结果缺失等情况。
6. 不新增不必要的重型依赖。
7. 优先使用 Vue 3 Composition API 和 `<script setup>`。
8. 优先使用项目现有 Element Plus、Pinia、Vue Router、ECharts 能力。

### 17.2 用户体验

1. 答题页不能一次展示所有题，要逐题答题。
2. 选项必须明显可点击。
3. 已选中状态必须明确。
4. 题号导航必须可点击跳转。
5. 结果页必须能看出「测评效果」，不能只显示一个分数。
6. 建设中页面不能空白，要有完整说明和返回入口。
7. 移动端不能横向溢出。

### 17.3 安全与稳定

1. 纯前端题库无需后端权限。
2. sessionStorage 写入前要 JSON.stringify。
3. JSON.parse 要 try/catch。
4. 倒计时组件卸载时要清理 interval。
5. 用户切换测评时要重置状态。
6. 结果页无数据时不能报错。

---

## 18. Skill 调用策略

> 下面是给 AICoder / Trae CN AI Coder 的明确 skill 使用建议。  
> 必须遵循用户提供的 `skill-router` 原则：先检查当前 available skills list，只能调用实际存在的 skill，不允许编造 skill；如果要使用 gstack 的 QA、browser testing、review 等能力，只调用顶层 `gstack`，不要把 QA/review 当成顶层 skill。

### 18.1 开始前必须做

```text
先调用 skill-router，读取当前 available skills list，并输出本任务的 Skill 使用计划。
```

如果当前环境没有 `skill-router`，则直接读取 available skills list，并按同样原则手动路由。

### 18.2 推荐 Skill 使用计划

#### 需求理解与规划

```text
skill-router
```

用途：

- 读取 available skills。
- 防止假调用。
- 制定本任务 skill 使用计划。
- 最终输出 skill 使用总结和验证方式。

#### 截图转页面结构

```text
image-to-code
```

用途：

- 根据用户提供的图 3、4、5、6、7 还原测评中心、答题页、结果页的信息结构。
- 不要求 1:1 像素复制，但要复用它的布局逻辑和交互关系。

#### 前端界面实现

```text
frontend-design
```

用途：

- 实现高质量 Vue 页面。
- 处理卡片布局、响应式布局、页面层级、按钮与状态。

#### UI/UX 抛光

```text
impeccable
```

用途：

- 优化间距、圆角、阴影、视觉层级、hover、动效和空状态。
- 检查是否像完整产品页面，而不是 demo 页面。

#### 审美增强

```text
gpt-taste
```

用途：

- 避免普通模板感。
- 强化「火花智创」平台的科技感、清爽感、卡片高级感。

#### 完整输出约束

```text
full-output-enforcement
```

用途：

- 确保所有新增文件和修改文件完整输出。
- 禁止省略代码、禁止 TODO、禁止「你自己补全」。

#### 评分逻辑测试

```text
tdd
```

用途：

- 对 `calculateAssessmentResult` 做基础测试。
- 至少测试：
  - 全 5 分结果为 50 分。
  - 反向题计分正确。
  - 维度分统计正确。
  - 未答题不能提交。

#### 浏览器验证 / 页面 QA

```text
gstack
```

用途：

- 使用 gstack 的 browser testing / QA 能力。
- 验证：
  - 首页入口是否出现。
  - `/assessment` 是否打开。
  - 分类切换是否正确。
  - 真实测评能答题和提交。
  - 结果页是否展示。
  - 建设中测评是否进入建设中页。
  - 移动端是否无横向溢出。

注意：

- 不要直接声称调用 `qa`、`review`、`browser testing` 顶层 skill。
- 只能调用顶层 `gstack`，并说明使用其内部 browser testing / QA 能力。
- 使用 gstack 前，按 skill-router 文档要求清理旧前后端进程、旧 Chrome DevTools MCP 进程、旧 MCP profile cache。

#### 图标统一

```text
better-icons
```

用途：

- 仅当 Element Plus 图标不够统一时使用。
- 用于在线测评卡片、分类侧栏、结果页图标。

#### 设计系统文档

```text
design-md
```

用途：

- 如果项目已有 `DESIGN.md` 或要求沉淀设计规范，则更新在线测评模块设计规则。
- 如果没有要求，不强制创建。

### 18.3 不建议使用的 skill

```text
industrial-brutalist-ui
```

原因：

- 本项目是高校创新创业平台，当前风格是蓝色科技、清爽平台化，不适合粗野主义 UI。

```text
minimalist-ui
```

原因：

- 可以参考它的留白和简洁，但本页面需要较明显的卡片和平台感，不应过度极简。

```text
prototype
```

原因：

- 用户要求真实可用功能，不是一次性原型。

---

## 19. AICoder 执行时的强约束

1. 必须先阅读本设计文档。
2. 必须结合项目 README 理解现有目录结构和技术栈。
3. 不要新增 React，不要改技术栈。
4. 使用 Vue 3 + Vite + Element Plus。
5. 本期不要强制新增后端，除非发现现有项目必须通过后端注册路由或数据。
6. 所有真实题库写在前端数据文件中。
7. 只做 5 个真实测评。
8. 每个真实测评必须 10 题。
9. 非真实测评必须能点击进入建设中页面。
10. 首页顶部导航和功能卡片都必须新增入口。
11. 答题页必须逐题答题，不要一次展示全部题。
12. 答题页必须有进度、题号导航、上一题、下一题、提交。
13. 结果页必须有分数、等级、综合评价、改进建议、维度分析。
14. 必须做基础响应式适配。
15. 必须实际运行或至少完成静态检查。
16. 最终必须输出变更文件清单、测试方式、skill 使用总结。

---

## 20. 建议验收清单

### 20.1 首页验收

- [ ] 顶部导航出现「在线测评」。
- [ ] 首页功能卡片出现「在线测评」。
- [ ] 两个入口都能跳转 `/assessment`。

### 20.2 测评中心验收

- [ ] 左侧分类显示完整。
- [ ] 创业测评有 4 个真实测评。
- [ ] 就业测评有 4 个建设中测评。
- [ ] 能力测评有 1 个真实测评和 3 个建设中测评。
- [ ] 搜索框可按标题过滤。
- [ ] 卡片 hover 有视觉反馈。
- [ ] 真实测评按钮显示「开始测评」。
- [ ] 建设中测评按钮显示「即将开放」或「查看详情」。

### 20.3 答题页验收

- [ ] 每个真实测评有 10 题。
- [ ] 每题选项可以点击。
- [ ] 选中状态明确。
- [ ] 上一题 / 下一题正常。
- [ ] 题号导航可跳转。
- [ ] 进度条正确。
- [ ] 未答完提交会提示。
- [ ] 答完提交会跳转结果页。
- [ ] 切换题目有动画效果。

### 20.4 结果页验收

- [ ] 显示总分。
- [ ] 显示等级。
- [ ] 显示总题数 / 得分。
- [ ] 显示完成率 / 得分率。
- [ ] 显示综合评价。
- [ ] 显示改进建议。
- [ ] 显示维度分析。
- [ ] 支持重新测评。
- [ ] 支持返回测评中心。

### 20.5 建设中页验收

- [ ] 非真实测评均进入建设中页。
- [ ] 页面显示测评名称。
- [ ] 页面有返回测评中心按钮。
- [ ] 页面不是空白页。

### 20.6 QA 验收

- [ ] 桌面端布局正常。
- [ ] 移动端布局正常。
- [ ] 控制台无明显报错。
- [ ] 路由刷新不崩溃。
- [ ] sessionStorage 缺失时结果页有兜底。
- [ ] 倒计时离开页面后清理。



---

## 21. 最终开发产物预期

AICoder 完成后，项目中至少应出现或修改：

```text
新增：
frontend/src/views/portal/OnlineAssessment.vue
frontend/src/views/portal/AssessmentDetail.vue
frontend/src/views/portal/AssessmentResult.vue
frontend/src/views/portal/AssessmentComingSoon.vue
frontend/src/data/assessmentBank.js

修改：
frontend/src/router/index.js
frontend/src/views/portal/PortalHome.vue
可能还会修改：
frontend/src/layouts/MainLayout.vue
frontend/src/components/SparkLogo.vue 或导航相关组件
frontend/src/styles/global.css 或 design-system.css
```

如果导航不在 `PortalHome.vue`，则根据现有项目实际结构找到顶部导航配置处修改。  
如果功能卡片数据是硬编码在 `PortalHome.vue` 中，就在那里新增卡片；如果抽成配置数组，就在配置数组中新增。

---

## 22. 风险提示

1. 当前参考网站可能需要登录，不能依赖线上页面抓取完整代码或数据，应以用户提供截图为主要视觉参考。
2. 真实测评题库是产品化简版，不应宣称具有专业心理测评资质。
3. 结果页文案应避免医疗、心理诊断类表述，只能说「能力画像」「发展建议」「倾向分析」。
4. 如果项目已有 AI 助手或 Live2D 浮层，注意不要遮挡答题页底部按钮。
5. 如果页面使用数字雨背景，测评答题页建议不使用过强背景，以免影响阅读。
6. 如果引入 ECharts 雷达图，要确认 ECharts 已在项目依赖中存在；README 显示项目已有 ECharts，可优先使用。

---

## 23. 一句话总结

本次新增的「在线测评」模块，应当是一个完整的产品功能：  
首页有入口，中心有分类和卡片，真实测评能答题，结果页能分析，未开放测评有建设中承接页，并且整体视觉与「火花智创 SparkAI」的蓝色科技、清爽卡片风格保持统一。
