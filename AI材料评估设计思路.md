# AI材料评估模块设计思路

## 1. 功能定位

在「火花智创 SparkAI Innovate」首页功能入口新增一个真实可用的功能卡片：**AI材料评估**。

该模块用于对创新创业项目材料进行智能化评估，帮助学生在正式报名、提交项目、路演答辩之前，提前发现材料中的结构问题、表达问题、亮点不足和改进方向。

本模块不是在线测评问卷，而是一个独立的「材料分析板块」。它与已有的在线测评功能并列存在，入口都放在首页功能卡片区域。

核心目标：

- 支持学生上传 PDF 材料。
- 分为两个互不绑定的评估入口：
  - **PPT评估**：用于评估路演PPT、展示PPT、项目介绍PPT导出的 PDF。
  - **报告评估**：用于评估商业计划书、项目申报书、项目报告导出的 PDF。
- 两个入口可以单独使用，互不依赖。
- 上传后进入 AI 分析进度页。
- 分析完成后出现「查看评分」按钮。
- 点击后进入评分报告页。
- 报告页带有“报告模块从四周飞入并组合成完整报告”的动画。
- 报告支持下载 PDF。
- 后端可以采用模拟评分策略，但前端和用户文案必须表现为正式的 AI 智能评估，绝不能暴露“随机、模拟、假数据”等字样。

---

## 2. 用户入口设计

### 2.1 首页功能卡片入口

在首页「功能入口」区域新增一张卡片，建议放在第三行第一张或与「AI项目助手」相邻的位置。

卡片信息：

- 标题：AI材料评估
- 副标题：智能评估PPT与项目报告
- 图标建议：文档分析、雷达图、柱状图、AI芯片、文件评分类图标
- 主色建议：蓝紫渐变或蓝青渐变
- 跳转路由：`/ai-material-evaluation`

视觉建议：

- 顶部细色条沿用首页卡片风格，但颜色可使用蓝紫渐变。
- 图标使用线性风格，与现有功能卡片统一。
- 卡片 hover 时增加轻微上浮、阴影增强、右侧箭头滑动。
- 卡片文案不要太长，保持首页整洁。

示例文案：

```text
AI材料评估
智能评估PPT与项目报告
```

### 2.2 路由归属

该页面属于平台能力入口，建议作为平台页处理，不显示左侧工作台侧边栏。

路由建议：

```js
{
  path: '/ai-material-evaluation',
  name: 'AIMaterialEvaluation',
  component: () => import('@/views/portal/AIMaterialEvaluation.vue'),
  meta: {
    title: 'AI材料评估',
    requiresAuth: true,
    platformPage: true
  }
}
```

如果当前项目的 `MainLayout.vue` 通过 `meta.platformPage` 区分平台页与工作台页，则此页面应设置 `platformPage: true`。

---

## 3. 页面整体流程

AI材料评估模块建议设计成 4 个阶段：

1. **功能首页 / 选择评估类型**
2. **上传材料**
3. **AI分析进度**
4. **评分报告展示与下载**

完整流程：

```text
首页功能卡片
  ↓
AI材料评估首页
  ↓
选择 PPT评估 / 报告评估
  ↓
上传对应 PDF 文件
  ↓
点击开始评估
  ↓
进入 AI 分析进度页
  ↓
分析完成，出现「查看评分」
  ↓
进入报告页
  ↓
报告模块飞入组合
  ↓
查看评分详情 / 下载PDF
```

---

## 4. AI材料评估首页设计

### 4.1 页面顶部 Hero 区

页面顶部应比参考网站更好看，建议采用「白底卡片 + 蓝紫渐变标题区 + 动态光斑」的设计。

标题建议：

```text
AI材料评估中心
```

副标题建议：

```text
上传PDF材料，获取PPT表现力与项目报告质量的智能评分建议
```

辅助说明：

```text
支持路演PPT、商业计划书、项目申报书等材料导出的PDF文件。
```

Hero 区视觉：

- 背景：浅蓝灰 `#f3f7ff` 或白色。
- 主卡片：圆角 24px，阴影柔和。
- 顶部标题区域：蓝到紫渐变。
- 可加入小型动态粒子、扫描线、浮动圆点，不要过度炫技。
- 页面整体风格要比参考网站更精致，避免过于空旷和简单。

### 4.2 类型选择卡片

页面中间放两个大卡片：

#### 卡片一：PPT评估

标题：

```text
PPT评估
```

说明：

```text
评估路演PPT的结构完整性、视觉表达、逻辑叙事、创新亮点呈现与答辩说服力。
```

适用材料：

```text
路演PPT、项目展示PPT、答辩PPT导出的 PDF
```

按钮：

```text
开始PPT评估
```

#### 卡片二：报告评估

标题：

```text
报告评估
```

说明：

```text
评估商业计划书或项目报告的内容完整度、市场论证、创新价值、实施路径与风险表达。
```

适用材料：

```text
商业计划书、项目申报书、项目报告导出的 PDF
```

按钮：

```text
开始报告评估
```

### 4.3 两个入口互不绑定

PPT评估和报告评估必须是两个独立流程：

- PPT 可以单独上传、单独评分、单独出报告。
- 报告可以单独上传、单独评分、单独出报告。
- 不要求用户同时上传 PPT 和报告。
- 不要出现“请先上传PPT再上传报告”这类绑定提示。
- 评分结果也分开保存，类型字段建议使用 `ppt` / `report`。

---

## 5. 上传页面设计

### 5.1 页面结构

选择评估类型后进入上传页，页面标题根据类型变化：

PPT评估：

```text
上传PPT材料
```

报告评估：

```text
上传报告材料
```

页面上方保留蓝色渐变标题条，形成专业流程感。

### 5.2 文件格式要求

本功能只处理 PDF 格式。

上传说明必须明确：

```text
仅支持 PDF 格式。请先将 PPT、商业计划书或项目报告导出为 PDF 后上传。
```

不支持：

- ppt
- pptx
- doc
- docx
- 图片
- zip
- rar

如果用户上传非 PDF，提示：

```text
当前仅支持 PDF 文件，请将材料导出为 PDF 后重新上传。
```

### 5.3 上传交互

建议使用 Element Plus 的 `el-upload` 或现有项目上传组件进行封装。

交互要求：

- 支持点击上传。
- 支持拖拽上传。
- 上传区有虚线边框、文件图标、格式提示。
- 上传成功后显示文件名、大小、删除按钮。
- 未上传文件时「开始评估」按钮禁用。
- 文件上传中显示 loading。
- 上传失败显示错误提示。
- 单个文件大小建议限制 50MB。
- 每次评估只上传一个 PDF 文件。

### 5.4 上传成功后的按钮

按钮文案：

```text
开始AI评估
```

点击后调用后端接口创建评估任务，并跳转到分析进度页。

---

## 6. AI分析进度页设计

### 6.1 页面目标

分析进度页要营造“系统正在认真分析材料”的感觉，但不需要后端真实长时间处理。

前端表现必须自然、可信、专业。

禁止出现：

- 模拟分析
- 随机评分
- 假进度
- demo数据
- 仅展示效果
- mocked

### 6.2 进度页视觉

参考用户提供的进度页截图，但要做得更高级：

- 中央大卡片。
- 顶部蓝紫渐变标题条。
- 中间使用旋转分析图标、AI扫描动画或环形进度。
- 下方显示百分比。
- 再下方显示阶段节点。
- 底部显示细进度条。
- 背景可使用浅色渐变和柔和光斑。

标题根据类型变化：

PPT：

```text
PPT表现力评估生成中
```

报告：

```text
项目报告评估生成中
```

核心状态文案：

```text
正在分析材料结构
系统正在处理您的PDF内容，请耐心等待...
```

### 6.3 进度阶段

PPT评估建议阶段：

1. PDF格式校验
2. 页面结构识别
3. 路演逻辑分析
4. 视觉表达评估
5. 创新亮点提取
6. 答辩说服力判断
7. 评分报告生成

报告评估建议阶段：

1. PDF格式校验
2. 文本内容提取
3. 项目结构识别
4. 市场与痛点分析
5. 创新价值评估
6. 风险与实施路径分析
7. 评分报告生成

### 6.4 前端进度策略

后端可以快速生成结果，但前端需要有完整过程感。

建议：

- 最短展示时间：6 秒。
- 最长展示时间：12 秒。
- 进度从 1% 到 100% 平滑增加。
- 阶段节点根据进度依次点亮。
- 每个阶段显示不同分析文案。
- 后端任务完成后，如果前端进度未到 100%，继续补齐动画。
- 前端进度到 100% 且接口返回成功后，显示「查看评分」按钮。

### 6.5 查看评分按钮

分析完成后，不要自动跳转，显示按钮：

```text
查看评分
```

按钮效果：

- 蓝色主按钮。
- 出现时带轻微弹出动画。
- 可加闪光扫过效果。
- 点击后进入报告页。

---

## 7. 评分报告页设计

### 7.1 页面目标

报告页是本模块最重要的视觉亮点。它要比参考页面更精致、更有产品感。

参考报告有蓝色顶部栏、总分、评分表、核心评价、问题分析和优化建议。本项目可以保留这种专业报告结构，但页面视觉应升级：

- 更现代的蓝紫渐变。
- 更好的留白。
- 更清晰的卡片层级。
- 更强的报告感。
- 加入图表和分区动画。
- 支持下载 PDF。

### 7.2 报告拼装动画

用户提出的重点动画：

> 报告各个区域从四周飞来，然后组合到一块儿，组合成一个报告。

实现建议：

- 报告页加载后，先显示浅色背景和中心标题。
- 随后各模块按顺序飞入：
  - 顶部报告封面从上方滑入。
  - 总分卡片从左侧滑入。
  - 雷达图或维度图从右侧滑入。
  - 评分明细表从下方滑入。
  - 核心评价从左下方滑入。
  - 优化建议从右下方滑入。
- 每个模块使用 `transform: translate(...) scale(...)` + `opacity` 过渡。
- 使用 `transition-delay` 形成拼装节奏。
- 总动画时长建议 900ms 到 1400ms。
- 动画完成后页面保持静态，不要一直晃动。
- 移动端可以简化为从下往上依次淡入。

CSS 思路：

```css
.report-section {
  opacity: 0;
  transform: translateY(32px) scale(0.98);
  transition: all .7s cubic-bezier(.22, 1, .36, 1);
}

.report-section.is-visible {
  opacity: 1;
  transform: translateY(0) scale(1);
}
```

也可以按模块添加不同初始方向：

```css
.from-top { transform: translateY(-48px); }
.from-left { transform: translateX(-64px); }
.from-right { transform: translateX(64px); }
.from-bottom { transform: translateY(64px); }
```

### 7.3 报告页顶部

顶部栏：

```text
AI材料评估报告
数据驱动 · 智能分析 · 精准优化
```

如果是 PPT：

```text
路演PPT评估报告
```

如果是报告：

```text
项目报告评估报告
```

右上角按钮：

```text
下载PDF
```

按钮不要太突兀，可以使用半透明白色按钮，hover 时增强。

### 7.4 报告核心模块

报告页建议包含以下模块：

#### 模块一：综合评分

内容：

- 总分：70–98
- 等级：
  - 90–98：优秀
  - 80–89：良好
  - 70–79：待提升
- 一句话评价
- 文件名
- 评估类型
- 生成时间

视觉：

- 圆环进度或大号数字分数。
- 分数使用蓝紫渐变。
- 等级使用标签。

#### 模块二：评分维度图

建议使用 ECharts：

- 雷达图
- 横向条形图
- 或评分卡片矩阵

PPT和报告各有独立维度。

#### 模块三：评分明细表

表格字段：

| 评分维度 | 得分 | 满分 | 评估说明 |

表格风格：

- 表头蓝色。
- 行背景交替浅灰。
- 文案不要太短，要有专业评语。
- 得分与满分居中。
- 评估说明左对齐。

#### 模块四：核心评价

结构：

```text
核心评价
本材料整体表现为……
```

内容要求：

- 150–250 字。
- 要根据总分和维度高低生成不同语气。
- 不要机械模板感太强。
- 尽量使用创新创业材料评估术语。

#### 模块五：主要优势

列出 3–5 条优势。

示例方向：

- 项目定位较清晰。
- 技术路线表达完整。
- 市场痛点有一定现实基础。
- 团队能力与项目方向匹配。
- 材料结构较完整。

#### 模块六：关键问题

列出 3–5 条问题。

示例方向：

- 用户痛点证据不足。
- 市场规模论证略显笼统。
- 商业模式闭环不够清晰。
- 风险应对缺少可执行方案。
- PPT页面信息密度不均衡。

#### 模块七：优化建议

列出 4–6 条建议。

建议要具体，不能只写“继续完善”。

例如：

```text
建议补充真实用户访谈、竞品对比或试点反馈数据，用事实增强痛点可信度。
```

#### 模块八：下一步行动

给用户一个可执行的行动清单，例如：

- 优先补充市场调研数据。
- 优化项目亮点页。
- 增加实施时间表。
- 精简过密页面。
- 将技术优势转化为用户价值表达。

---

## 8. PPT评估评分维度

PPT评估重点是“展示表达与路演说服力”，不是完整商业计划书审查。

建议满分 100 分，维度如下：

| 维度 | 满分 | 说明 |
|---|---:|---|
| 结构完整性 | 12 | 是否包含项目背景、痛点、方案、市场、团队、计划等必要页面 |
| 视觉版式与阅读体验 | 14 | 页面是否整洁、层级是否清晰、图文比例是否合理 |
| 痛点表达清晰度 | 12 | 是否讲清楚目标用户、真实问题和需求场景 |
| 解决方案呈现 | 14 | 是否清楚展示产品、技术、服务或商业模式 |
| 创新亮点表达 | 14 | 是否突出技术创新、模式创新或应用场景创新 |
| 商业与市场逻辑 | 12 | 是否说明市场空间、用户价值、落地路径和增长逻辑 |
| 团队与执行计划 | 10 | 是否展示团队分工、能力匹配和推进计划 |
| 路演说服力 | 12 | 是否适合现场表达，是否具备打动评委的叙事节奏 |

### PPT关键词参考

后端可以对 PDF 文本进行轻量关键词识别：

```text
痛点、用户、市场、规模、竞品、商业模式、盈利、技术路线、创新点、团队、落地、试点、融资、计划、里程碑、风险、优势、专利、数据、案例
```

命中关键词越多，可轻微提高相关维度基础分。但不要让关键词规则过于明显。

---

## 9. 报告评估评分维度

报告评估重点是“内容完整度、论证深度与商业可行性”。

建议满分 100 分，维度如下：

| 维度 | 满分 | 说明 |
|---|---:|---|
| 项目摘要与定位 | 10 | 是否准确说明项目要解决什么问题、面向谁、价值是什么 |
| 市场痛点与需求论证 | 12 | 是否有调研、数据、用户场景、行业背景支撑 |
| 创新价值与技术路线 | 14 | 是否体现技术、产品、模式或服务创新 |
| 商业模式与可持续性 | 14 | 是否说明收入来源、成本结构、客户获取和增长逻辑 |
| 实施路径与里程碑 | 12 | 是否有清晰阶段目标、任务拆解和落地计划 |
| 团队能力与资源匹配 | 10 | 团队背景、分工、资源是否支撑项目推进 |
| 财务、风险与应对 | 14 | 是否说明资金、成本、风险、竞争与解决策略 |
| 材料规范与论证完整性 | 14 | 结构、格式、表达、图表、引用和逻辑是否完整 |

### 报告关键词参考

```text
项目背景、行业痛点、用户需求、市场规模、竞品分析、商业模式、收入来源、成本结构、技术路线、创新点、实施计划、里程碑、团队分工、风险分析、财务预测、社会价值、推广策略、调研数据、专利、成果转化
```

---

## 10. 后端评分策略

### 10.1 原则

用户看到的是 AI 智能评估，后端可以采用轻量模拟逻辑。

后端不要写入用户可见文案：

- 随机
- 模拟
- 假数据
- mock
- demo
- placeholder

代码内部可以使用模拟评分，但对外接口统一返回正式评估结果。

### 10.2 总分生成

总分范围：

```text
70–98
```

分布要求：

- 分数不要太低。
- 大多数分数集中在 80–90。
- 少量结果在 70–79。
- 少量结果在 91–98。
- 呈近似正态分布。

建议实现：

```python
import random

def normal_score(mean=84, std=6, min_score=70, max_score=98):
    score = random.gauss(mean, std)
    return max(min_score, min(max_score, round(score)))
```

如果关键词命中较多，可以加 1–4 分；如果 PDF 文本提取为空，可以减 1–3 分，但最低仍不低于 70。

### 10.3 维度分生成

维度分要与总分看起来一致。

思路：

1. 先为每个维度生成一个 0.75–0.98 的比例。
2. 根据维度满分计算初始得分。
3. 用总分对维度总和进行微调。
4. 保证每个维度分不超过满分。
5. 保证所有维度分相加接近总分。

维度得分不要完全平均，要有高低差，这样报告更真实。

### 10.4 术语库

后端准备多套术语池，按评估类型、维度、得分等级随机组合。

术语库至少包含：

- 综合评价模板
- 优势表达模板
- 问题表达模板
- 优化建议模板
- 维度评语模板
- 下一步行动建议模板

#### 综合评价模板示例

```text
该材料整体结构较为完整，能够较清楚地呈现项目定位、核心方案与推进思路。当前材料已经具备参赛展示基础，但在市场数据支撑、创新亮点凝练和风险应对方面仍有进一步提升空间。
```

```text
该材料具备较好的项目表达基础，逻辑主线相对清晰，核心价值能够被快速识别。建议后续进一步强化真实用户场景、量化数据和阶段性成果展示，以提升评审说服力。
```

```text
该材料在项目愿景和团队执行意愿方面表现积极，但部分论证仍停留在概念描述层面。建议围绕目标用户、落地路径和商业闭环补充更具体的证据。
```

#### 优势模板示例

```text
项目定位较明确，能够围绕目标场景展开材料表达。
```

```text
材料结构具备基本完整性，评审能够较快理解项目主线。
```

```text
团队能力与项目方向具有一定匹配度，具备继续打磨和推进基础。
```

```text
创新点已有初步呈现，适合进一步通过案例、数据或原型成果强化。
```

#### 问题模板示例

```text
市场调研证据略显不足，缺少对目标用户真实需求的量化支撑。
```

```text
商业模式描述仍偏概念化，收入来源、成本结构和增长路径需要进一步明确。
```

```text
风险应对方案不够具体，建议补充技术、市场、运营和团队层面的预案。
```

```text
部分页面信息密度偏高，重点内容不够突出，可能影响评委快速理解。
```

#### 优化建议模板示例

```text
建议补充用户访谈、问卷调研、试点反馈或竞品对比数据，让痛点判断更有依据。
```

```text
建议将创新点拆分为“技术创新、模式创新、应用场景创新”三个层面分别表达。
```

```text
建议增加阶段性里程碑，用时间轴展示项目从研发、测试到推广的推进路径。
```

```text
建议将团队分工与项目关键任务绑定，突出成员能力如何支撑项目落地。
```

---

## 11. PDF解析设计

### 11.1 后端解析方式

项目后端已有 PDF 文档解析相关依赖，可以优先复用现有 `document_parser.py` 或新增独立解析函数。

解析目标不是做深度语义理解，而是获取基础文本、页数、关键词和内容长度。

建议提取字段：

```json
{
  "page_count": 12,
  "text_length": 8500,
  "keyword_hits": {
    "market": 8,
    "team": 4,
    "risk": 3
  },
  "readability_level": "good"
}
```

### 11.2 PDF不可解析情况

如果 PDF 是扫描件或文字提取为空，也允许生成评估报告。

处理策略：

- 评分略微降低 1–3 分。
- 增加一条建议：

```text
当前PDF可提取文本较少，建议上传由原始文档直接导出的PDF，以便系统更准确识别文本结构和关键信息。
```

---

## 12. 下载PDF设计

### 12.1 下载入口

报告页右上角提供：

```text
下载PDF
```

点击后下载当前评估报告。

### 12.2 PDF内容结构

下载的 PDF 应尽量接近页面报告结构，但不必完全保留网页动画。

PDF内容建议：

1. 封面标题
2. 项目材料名称
3. 评估类型
4. 生成时间
5. 综合评分
6. 评分维度表
7. 核心评价
8. 主要优势
9. 关键问题
10. 优化建议
11. 下一步行动

### 12.3 PDF生成方式

推荐后端生成 PDF，便于统一样式和下载体验。

可选方案：

#### 方案一：后端使用 ReportLab

优点：

- 稳定。
- 适合生成表格和长文本。
- 不依赖浏览器截图。
- 下载体验可控。

需要新增依赖：

```text
reportlab
```

后端新增接口：

```text
GET /api/material-evaluation/reports/<report_id>/pdf
```

#### 方案二：前端使用 html2canvas + jsPDF

优点：

- 更接近页面视觉。
- 实现快。

缺点：

- 长页面分页容易出问题。
- 中文字体和清晰度需要处理。
- 不如后端稳定。

建议优先使用方案一。

---

## 13. 数据结构设计

### 13.1 评估任务表

建议新增模型：`MaterialEvaluationTask`

字段建议：

```text
id
user_id
evaluation_type        # ppt / report
file_name
file_path
file_size
status                 # uploaded / analyzing / completed / failed
progress
score
level
summary
created_at
updated_at
completed_at
```

### 13.2 评估报告表

建议新增模型：`MaterialEvaluationReport`

字段建议：

```text
id
task_id
user_id
evaluation_type
total_score
level
dimension_scores_json
core_comment
advantages_json
problems_json
suggestions_json
next_actions_json
pdf_path
created_at
```

如果想减少数据库复杂度，也可以只建一张表，将报告内容以 JSON 字段存储。

---

## 14. API设计

### 14.1 上传材料

```http
POST /api/material-evaluation/upload
```

请求：

- multipart/form-data
- file: PDF 文件
- evaluation_type: ppt / report

返回：

```json
{
  "code": 200,
  "message": "上传成功",
  "data": {
    "task_id": 1,
    "file_name": "项目路演PPT.pdf",
    "evaluation_type": "ppt"
  }
}
```

### 14.2 开始评估

```http
POST /api/material-evaluation/tasks/<task_id>/analyze
```

返回：

```json
{
  "code": 200,
  "message": "评估任务已开始",
  "data": {
    "task_id": 1,
    "status": "analyzing"
  }
}
```

### 14.3 查询任务状态

```http
GET /api/material-evaluation/tasks/<task_id>
```

返回：

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "task_id": 1,
    "status": "completed",
    "progress": 100,
    "report_id": 1,
    "score": 86
  }
}
```

### 14.4 获取报告详情

```http
GET /api/material-evaluation/reports/<report_id>
```

返回：

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "id": 1,
    "evaluation_type": "ppt",
    "file_name": "项目路演PPT.pdf",
    "total_score": 86,
    "level": "良好",
    "dimension_scores": [],
    "core_comment": "",
    "advantages": [],
    "problems": [],
    "suggestions": [],
    "next_actions": []
  }
}
```

### 14.5 下载PDF

```http
GET /api/material-evaluation/reports/<report_id>/pdf
```

返回：

- application/pdf
- Content-Disposition: attachment

---

## 15. 前端文件规划

建议新增：

```text
frontend/src/views/portal/AIMaterialEvaluation.vue
frontend/src/views/portal/AIMaterialEvaluationUpload.vue
frontend/src/views/portal/AIMaterialEvaluationProgress.vue
frontend/src/views/portal/AIMaterialEvaluationReport.vue
frontend/src/api/materialEvaluation.js
frontend/src/components/material-evaluation/EvaluationTypeCard.vue
frontend/src/components/material-evaluation/PdfUploadBox.vue
frontend/src/components/material-evaluation/AnalysisProgressCard.vue
frontend/src/components/material-evaluation/AnimatedReportSection.vue
frontend/src/components/material-evaluation/ScoreRing.vue
frontend/src/components/material-evaluation/DimensionScoreTable.vue
```

如果想减少文件数量，也可以先放在一个 view 内完成，但建议至少拆出上传、进度、报告几个组件，避免页面过大。

---

## 16. 后端文件规划

建议新增：

```text
backend/routes/material_evaluation.py
backend/services/material_evaluation_service.py
backend/services/material_report_pdf.py
backend/models/material_evaluation.py
```

并在 `app.py` 或蓝图注册处注册：

```python
from routes.material_evaluation import material_evaluation_bp
app.register_blueprint(material_evaluation_bp, url_prefix='/api/material-evaluation')
```

如果项目模型统一在 `models/__init__.py` 导出，需要补充导出。

---

## 17. 视觉风格要求

整体风格要适配「火花智创 SparkAI」现有设计，不要直接照搬参考网站。

关键词：

- 清爽
- 科技感
- 蓝紫渐变
- 白底大卡片
- 柔和阴影
- 圆角
- 动态但克制
- 专业报告感

建议色彩：

```css
--material-primary: #2563eb;
--material-secondary: #7c3aed;
--material-cyan: #06b6d4;
--material-bg: #f5f8ff;
--material-card: #ffffff;
--material-text: #1e293b;
--material-muted: #64748b;
```

动效建议：

- 卡片 hover 上浮 4px。
- 上传区拖拽时边框变蓝。
- 分析进度使用旋转图标 + 进度条。
- 报告模块进入时飞入拼装。
- 下载按钮 hover 时有轻微发光。

---

## 18. 空状态与异常状态

### 18.1 未上传

```text
请先上传PDF材料后再开始评估
```

### 18.2 格式错误

```text
当前仅支持PDF格式，请重新上传
```

### 18.3 上传失败

```text
文件上传失败，请检查网络后重试
```

### 18.4 分析失败

```text
AI材料评估暂时未完成，请稍后重试
```

### 18.5 报告不存在

```text
未找到对应的评估报告
```

---

## 19. 权限与安全

- 上传和评估接口需要 JWT 登录。
- 用户只能查看自己的评估任务和报告。
- 管理员可以后续扩展查看所有报告。
- 文件名要做安全处理。
- 限制 PDF 文件大小。
- 后端检查扩展名和 MIME 类型。
- 下载 PDF 时也要检查权限。
- 不要将用户上传路径直接暴露为可猜测路径。

---

## 20. 实现优先级

### 第一阶段：核心闭环

- 首页新增 AI材料评估卡片。
- 新增 AI材料评估首页。
- 支持 PPT评估 / 报告评估入口。
- 支持上传 PDF。
- 支持分析进度展示。
- 支持生成评分报告。
- 支持查看评分。
- 支持报告页动画。

### 第二阶段：报告下载

- 后端生成 PDF。
- 报告页下载 PDF。
- PDF样式优化。

### 第三阶段：体验优化

- ECharts 图表。
- 历史评估记录。
- 重新评估。
- 报告分享。
- 与项目材料中心联动。

---

## 21. 注意事项

1. PPT评估和报告评估必须互不绑定。
2. 只支持 PDF。
3. 前端不能出现“模拟、随机、mock、demo”等提示。
4. 后端评分可以模拟，但用户可见结果必须专业可信。
5. 总分保持 70–98，整体近似正态分布。
6. 报告文案要准备多套术语，避免每次生成完全一样。
7. 报告动画是重点，必须实现“模块从四周飞入并组合成报告”的效果。
8. 下载 PDF 是必做能力。
9. 页面要比参考网站更美观，不要只是复刻。
10. 优先保证主流程完整，再优化细节。
