# SparkAI AI助手窗口亮色化与拖拽缩放体验优化设计思路（精简含 Skills 版）

> 目标：把当前「火花 AI 助手」浮窗从暗色、厚重、不协调的样式，改造成亮色、清爽、科技感、亲和型 AI 助手窗口；同时修复拖拽缩放方向反直觉的问题。  
> 说明：本文件只写设计方向、交互原则、Skill 调用要求和验收标准，不嵌入完整启动提示词，不写死具体 CSS。启动提示词请由用户单独发送。

---

## 0. 重要执行原则

本次任务不是改 AI 能力，而是改 AI 助手浮窗的 UI/UX 与拖拽缩放体验。

必须保留现有功能：

- 普通 AI 对话
- 语音输入
- 清空对话
- 发送消息
- 展开 / 收起 / 关闭
- 项目智能体模式
- 智能引航
- 材料问答
- 商业计划书体检
- 路演稿生成
- 模拟路演答辩
- 现有接口调用逻辑
- 现有登录态、用户态相关逻辑

不要大范围重写，不要为了改 UI 破坏业务逻辑。

---

## 1. 本次任务必须调用的本地 Skills

AI Coder 开始编码前，必须主动识别并调用下列本地 Skills。Skill 名称必须严格按本地文件夹名写，不要写模糊名称。

| 本地 Skill 文件夹名 | 何时调用 | 用途 |
|---|---|---|
| `redesign-existing-projects` | 任务开始时 | 审计现有 AI 助手窗口结构，在不破坏功能的前提下改造现有项目 |
| `frontend-design` | 规划窗口结构时 | 设计 Header、Body、Composer、智能体卡片、响应式布局 |
| `design-md` | 确认视觉方向时 | 对齐 SparkAI 当前设计系统和整体平台风格 |
| `impeccable` | 做视觉抛光时 | 检查间距、圆角、阴影、按钮、输入框、状态反馈、可访问性 |
| `better-icons` | 替换图标时 | 统一 AI 助手、语音、清空、发送、展开、收起、缩放图标 |
| `design-taste-frontend` | 设计欢迎页、快捷按钮、聊天区时 | 提升前端审美，避免普通模板感 |
| `high-end-visual-design` | 做最终视觉质感时 | 增强亮色玻璃、柔和渐变、高级留白和层级 |
| `ui-ux-pro-max` | 处理拖拽、缩放、移动端体验时 | 修复缩放方向反直觉、移动端适配、窗口状态持久化 |
| `full-output-enforcement` | 每一阶段完成后 | 检查是否漏改、是否有 TODO、undefined、半成品、控制台报错 |

不要使用：

| 本地 Skill 文件夹名 | 原因 |
|---|---|
| `industrial-brutalist-ui` | 当前 SparkAI 不是粗野主义风格 |
| `image-to-code` | 本次不是按图还原 |
| `imagegen-frontend-web` | 本次不需要生成参考图 |
| `imagegen-frontend-mobile` | 本次不需要生成移动端参考图 |
| `brandkit` | 本次不重做品牌视觉 |

---

## 2. 必须生成 Skill 使用记录

为了确认 AI Coder 真的使用了 Skills，本次任务必须在项目根目录生成一个新的 Markdown 记录文件：

```txt
SKILL_USAGE_LOG_AI_ASSISTANT.md
```

每次使用 Skill，都必须追加一条记录。

### 记录格式

```md
## Skill 使用记录

### 记录 1
- 时间：YYYY-MM-DD HH:mm:ss
- 当前任务：例如，审计 AI 助手窗口结构
- 调用 Skill：redesign-existing-projects
- 该 Skill 提供的帮助：例如，帮助识别现有组件结构、保留业务逻辑、避免大范围重写
- 产出结果：例如，确认需要修改 HuahuoAssistant.vue 和相关样式文件

### 记录 2
- 时间：YYYY-MM-DD HH:mm:ss
- 当前任务：例如，设计亮色玻璃风格
- 调用 Skill：impeccable、high-end-visual-design
- 该 Skill 提供的帮助：例如，帮助优化视觉层级、圆角、阴影、留白和按钮状态
- 产出结果：例如，完成亮色窗口视觉方向
```

### 记录要求

- 时间必须写真实执行时刻，格式为 `YYYY-MM-DD HH:mm:ss`。
- 不允许只写“已使用多个 Skills”。
- 必须说明每个 Skill 对当前步骤提供了什么帮助。
- 如果一个步骤用了多个 Skills，要分别说明它们的作用。
- 最终提交前必须更新这份记录文件。
- 如果没有生成这个文件，视为任务未完成。

---

## 3. 当前问题

### 3.1 AI 助手窗口太暗

当前窗口是大面积深蓝黑暗色面板，问题包括：

- 和 SparkAI 当前亮色页面不协调。
- 面板面积大，暗色压迫感强。
- 标题栏、内容区、输入区层级不够清晰。
- 欢迎语和快捷按钮看起来不够精致。
- 项目智能体能力卡片偏后台工具风。
- 图标风格不统一。
- 输入区不够像现代 AI Chat 输入区。

### 3.2 缩放方向反直觉

当前缩放点在左上角，导致体验很怪：

- 鼠标从左上往右下拖，窗口可能反而变窄。
- 鼠标从下往上拖，窗口可能反而变矮。
- 用户更习惯从右下角缩放窗口。
- 右下角拖动时，鼠标往右下，窗口自然变大，这才符合直觉。

---

## 4. 视觉设计方向

### 4.1 总体风格

AI 助手窗口改成：

```txt
亮色玻璃 / 青蓝渐变 / 轻阴影 / 大圆角 / 卡片化 / 高级留白 / 亲和 AI
```

应该看起来像 SparkAI 平台里的「智能助理浮窗」，而不是后台管理弹窗。

### 4.2 颜色方向

不要写死具体 CSS，但整体建议：

- 主背景：白色、浅蓝白、浅青白
- 主色：青蓝、蓝紫
- 辅助色：浅灰蓝、淡紫光晕
- 文字：深蓝黑、灰蓝
- 边框：浅青蓝半透明
- 阴影：柔和、轻量、空间感

### 4.3 Header 方向

Header 要做到：

- 左侧显示「火花 AI 助手」和统一 AI 图标。
- 右侧显示模式切换、语音、最小化、关闭等操作。
- 不要堆 emoji。
- 当前模式用轻量 pill 高亮。
- Header 空白区域可以拖动窗口。
- 按钮区域不能触发拖动。

### 4.4 Body 方向

默认欢迎页建议表达：

```txt
你好，我是火花
你的创新创业 AI 助手

我可以帮你生成项目简介、推荐竞赛、优化商业计划书、模拟路演答辩。
```

快捷问题建议改成浅色 pill 按钮：

```txt
帮我生成项目简介
竞赛有哪些赛道？
如何组建团队？
帮我优化商业计划书
```

消息区建议：

- 用户消息靠右，用青蓝渐变气泡。
- AI 消息靠左，用白色卡片气泡。
- 消息列表要有足够留白。
- 空状态不要空得太夸张。

### 4.5 项目智能体模式方向

项目智能体模式改成亮色能力卡片：

```txt
智能引航
AI材料问答
商业计划书体检
路演稿生成
AI评审辅助
模拟路演答辩
批量审核助手
智能反馈生成
```

要求：

- 能力卡片是白底 / 浅色卡片。
- 图标统一。
- 标题清晰。
- 描述简短。
- hover 有轻微上浮和边框高亮。
- 保留原有点击逻辑。

### 4.6 输入区方向

输入区要像现代 AI Chat Composer：

- 输入框明亮、圆角、聚焦时有青蓝高亮。
- 发送按钮明显但不突兀。
- 语音、清空、更多按钮用统一圆形 icon button。
- 输入区固定底部，和消息区层级分明。
- 不要使用深色输入栏。

---

## 5. 拖拽与缩放交互方向

### 5.1 缩放点位置

取消左上角作为主要缩放点。

推荐：

```txt
右下角 resize handle
```

左上角的装饰线可以保留，但只做装饰或拖拽提示，不做缩放入口。

### 5.2 缩放方向

右下角缩放必须符合直觉：

| 鼠标动作 | 期望结果 |
|---|---|
| 向右拖 | 窗口变宽 |
| 向下拖 | 窗口变高 |
| 向右下拖 | 窗口变大 |
| 向左上拖 | 窗口变小 |

### 5.3 交互分离

必须区分移动和缩放：

| 操作 | 触发区域 |
|---|---|
| 移动窗口 | Header 空白区域 |
| 缩放窗口 | 右下角 resize handle |
| 点击按钮 | 不触发移动 |
| 输入框输入 | 不触发移动 |
| 能力卡片点击 | 不触发移动 |

### 5.4 尺寸限制

需要设置合理的最小和最大尺寸：

- 最小宽度不能小到内容挤坏。
- 最小高度不能小到输入区遮住内容。
- 最大宽高不能超过视口。
- 缩放后窗口不能跑出屏幕。
- 旧 localStorage 数据异常时要自动重置。

### 5.5 移动端

移动端不建议自由缩放：

- 隐藏 resize handle。
- 窗口宽度接近屏幕宽度。
- 高度建议为 70vh 到 80vh。
- 保留关闭、收起、发送等核心操作。
- 不要出现横向溢出。

---

## 6. 建议审计的关键词和文件

AI Coder 必须先搜索相关组件，不能硬猜文件名。

建议搜索关键词：

```txt
火花 AI 助手
AI助手
AI 助手
Huahuo
Assistant
Agent
VoiceChat
resize
drag
mousedown
mousemove
localStorage
```

可能涉及文件：

```txt
frontend/src/components/
frontend/src/views/
frontend/src/layouts/
frontend/src/composables/
frontend/src/styles/
```

实际以搜索结果为准。

---

## 7. 分步骤 Prompt

### Prompt 1：审计现有 AI 助手

请先搜索并审计当前「火花 AI 助手」浮窗相关组件，确认窗口组件、样式文件、拖拽逻辑、缩放逻辑、localStorage 持久化逻辑、普通对话模式、项目智能体模式、语音按钮、清空按钮所在位置。不要立刻改代码。

必须调用并记录 Skills：

- `redesign-existing-projects`
- `frontend-design`
- `ui-ux-pro-max`
- `full-output-enforcement`

并在根目录 `SKILL_USAGE_LOG_AI_ASSISTANT.md` 记录本步骤使用情况。

### Prompt 2：重做亮色窗口视觉

请将当前暗色 AI 助手窗口改成亮色玻璃风格。不要写成后台弹窗，要像 SparkAI 平台内的智能助理浮窗。整体方向为白色/浅蓝玻璃、青蓝渐变、轻阴影、大圆角、清晰层级、亲和科技感。保留原有功能和组件逻辑。

必须调用并记录 Skills：

- `design-md`
- `impeccable`
- `design-taste-frontend`
- `high-end-visual-design`

### Prompt 3：优化欢迎页、快捷按钮和消息区

请优化默认欢迎页、快捷问题按钮、消息气泡和空状态。欢迎区要亲和、清爽、有层级；快捷按钮使用浅色 pill 风格；用户消息和 AI 消息要有明确视觉区分。不要使用大面积暗色。

必须调用并记录 Skills：

- `frontend-design`
- `impeccable`
- `design-taste-frontend`

### Prompt 4：优化项目智能体模式

请将项目智能体模式改成亮色能力卡片网格。保留所有能力入口和点击逻辑。每个卡片使用统一图标、标题、简短描述、hover 状态和选中状态。

必须调用并记录 Skills：

- `better-icons`
- `frontend-design`
- `impeccable`
- `ui-ux-pro-max`

### Prompt 5：修复拖拽缩放方向

请取消左上角作为主要缩放点，改为右下角 resize handle。右下角拖动时，鼠标向右窗口变宽，向下窗口变高，向右下窗口变大，向左上窗口变小。缩放逻辑必须符合用户直觉。

必须调用并记录 Skills：

- `ui-ux-pro-max`
- `frontend-design`
- `redesign-existing-projects`

### Prompt 6：分离移动与缩放

请确保 Header 空白区域负责移动窗口，右下角 resize handle 负责缩放窗口。点击按钮、输入框、快捷按钮、能力卡片时不能触发拖动。resize 和 drag 不能互相冲突。

必须调用并记录 Skills：

- `ui-ux-pro-max`
- `impeccable`
- `full-output-enforcement`

### Prompt 7：移动端和持久化

请检查移动端体验和窗口状态持久化。移动端隐藏 resize handle，避免横向溢出。localStorage 保存窗口位置和尺寸时要处理旧数据异常，避免窗口飞出屏幕或尺寸错误。

必须调用并记录 Skills：

- `ui-ux-pro-max`
- `frontend-design`
- `full-output-enforcement`

### Prompt 8：最终验收

请逐项测试普通对话、项目智能体、语音、清空、发送、关闭、展开、拖拽移动、右下角缩放、刷新恢复、移动端显示、控制台报错。发现问题必须修复，不要留下 TODO 或半成品。

必须调用并记录 Skills：

- `full-output-enforcement`
- `impeccable`
- `redesign-existing-projects`

---

## 8. 验收标准

### 8.1 Skill 调用记录

- 根目录必须存在 `SKILL_USAGE_LOG_AI_ASSISTANT.md`。
- 每个步骤必须记录调用了哪些 Skills。
- 每条记录必须包含时间、当前任务、调用 Skill、帮助内容、产出结果。
- 不能只写“已使用 Skills”。

### 8.2 视觉验收

- AI 助手窗口不再是大面积暗色。
- 整体变成亮色、清爽、科技感、亲和型 AI 助手。
- Header、Body、Composer 层级清晰。
- 输入框、按钮、快捷问题、能力卡片统一协调。
- 图标风格统一，不再依赖杂乱 emoji。
- 圆角、阴影、边框、留白精致。
- 和 SparkAI 平台整体视觉一致。

### 8.3 交互验收

- 右下角拖拽缩放符合直觉。
- 鼠标向右，窗口变宽。
- 鼠标向下，窗口变高。
- 鼠标向右下，窗口变大。
- 鼠标向左上，窗口变小。
- 不再出现左上角反向缩放问题。
- Header 可拖动窗口。
- 点击按钮、输入框、能力卡片不会触发拖动。
- 缩放和拖动不会互相冲突。
- 窗口不会超出屏幕。
- 刷新后窗口位置和尺寸可恢复。
- 旧 localStorage 数据异常时能自动重置。

### 8.4 功能验收

- 普通 AI 对话可用。
- 项目智能体能力入口可用。
- 智能引航可用。
- 材料问答、商业计划书体检、路演稿生成等入口不被破坏。
- 语音按钮不被破坏。
- 清空按钮不被破坏。
- 关闭 / 展开 / 收起功能正常。
- 控制台无报错。
- 没有 undefined。
- 没有 TODO 占位。
- 移动端正常显示。

---

## 9. 给 AI Coder 的一句话重点

```txt
这次不是改 AI 能力，而是改 AI 助手浮窗的 UI/UX：暗色改亮色，窗口变精致，缩放点改到右下角，缩放方向必须符合用户直觉，原有功能全部保留，并且每次使用本地 Skill 都要写入根目录 SKILL_USAGE_LOG_AI_ASSISTANT.md。
```
