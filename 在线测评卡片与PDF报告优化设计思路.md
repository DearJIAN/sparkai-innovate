# 在线测评卡片视觉升级与 PDF 报告下载修复设计思路

> 适用对象：火花智创 SparkAI Innovate
> 任务性质：UI 体验升级 + PDF 报告模板重构 + 下载链路 Bug 修复
> 重要限制：本文档不依赖截图理解，AICoder 只需要按文字说明执行即可。

---

## 1. 本次要解决的问题

本次不是新增大功能，而是对已经实现的「在线测评」与「AI 材料评估 / 报告下载」做体验升级和 Bug 修复。

需要重点解决三个问题：

1. **在线测评卡片顶部大字太普通**

   - 当前卡片顶部是大面积渐变色块，中间放白色大字。
   - 这种表现方式太像普通模板，没有设计记忆点。
   - 需要改成更有辨识度、更有“被设计过”的卡片视觉。
2. **下载后的 PDF 报告质量太差**

   - 当前 PDF 报告首页视觉重心失衡，像临时拼出来的封面。
   - 报告内容区域存在信息不完整、层级弱、排版空洞、表格观感差等问题。
   - 需要把 PDF 报告做成真正像“专业评估报告”的成品。
3. **每次点击 PDF 下载，会先下载一个 0KB PDF，再下载实际 PDF**

   - 这是明确 Bug。
   - 修复目标是：用户每点击一次下载，只触发一次有效下载，只生成一个非空 PDF 文件。

---

## 2. Skill 使用要求

AICoder 开始前必须读取当前项目的 skill-router V2 规则，并由 skill-router 自己判断当前阶段要调用哪些 skill。

本设计思路不限制最终视觉风格，只描述目标、问题和验收标准。
具体视觉方向由 skill-router V2 组织合适的 UI / 设计 / QA / Debug skills 来决定。

推荐但不强制的 skill 方向如下：

- `skill-router`：必须先用，用于分阶段选择 skills。
- `zoom-out`：先拉高抽象层，避免只修局部样式。
- `design-taste-frontend`：用于工程可落地的前端视觉方向。
- `ui-ux-pro-max`：用于专业 UI/UX 判断。
- `high-end-visual-design`：用于生成更高级的视觉方向。
- `frontend-design`：用于实际前端页面与组件改造。
- `impeccable`：用于 UI 抛光、间距、层级、动效优化。
- `better-icons`：用于替换或补充卡片图标、报告图标。
- `diagnose`：用于定位 PDF 下载 0KB 的根因。
- `full-output-enforcement`：用于避免只改半截、漏掉接口或漏掉异常处理。
- `tdd`：用于最小化验证下载逻辑和评分报告生成逻辑。
- `gstack`：用于最终浏览器 QA；如果使用其浏览器能力，只能调用顶层 `gstack`，不能声称直接调用内部 qa/review 子 skill。

---

## 3. 在线测评卡片视觉升级

### 3.1 当前问题

当前在线测评卡片的核心问题不是颜色，而是表达方式太直白：

- 顶部是大渐变块。
- 中间放白色大字。
- 所有卡片结构过于相似。
- 视觉上没有“创业测评 / 能力画像 / 智能分析”的特点。
- 卡片之间缺少主题差异，用户看久后容易审美疲劳。

### 3.2 设计目标

新的卡片应该满足：

1. 每张卡片仍然清楚表达测评名称。
2. 不再使用“纯色块 + 居中白字”的普通模板。
3. 每类测评拥有不同的主题视觉符号。
4. 卡片整体仍然简洁，不要变成花里胡哨的海报。
5. 视觉上要更有科技感、分析感、创业感和平台品牌感。
6. 卡片在列表中要有辨识度，用户一眼能区分不同测评。

### 3.3 推荐改造方向：主题化卡片视觉

建议新增一个可复用组件：

```text
frontend/src/components/assessment/AssessmentVisualCard.vue
```

或者在现有在线测评页面内拆出类似组件。

组件职责：

- 接收测评标题、分类、描述、题数、时长、浏览量、主题类型。
- 根据主题类型渲染不同的顶部视觉。
- 顶部视觉不是静态大字，而是由“图形 + 字形 + 微动效 + 主题符号”组合出来。

### 3.4 顶部视觉的可选方案

AICoder 不必全部实现，可从下面选一种或组合使用。

#### 方案 A：SVG 主题字卡

用 SVG 绘制每张卡片顶部的主题字和装饰元素。

例如：

- 创业精神：火花、上升箭头、能量线、燃点扩散。
- 创业性格：人格雷达、圆环、坐标点、内外圈。
- 创业兴趣：指南针、路径线、探索节点。
- 创业能力：数据曲线、柱状图、能力矩阵。
- 团队合作能力：节点网络、连接线、协作气泡。

实现建议：

```text
AssessmentVisualCard.vue
AssessmentWordArt.vue
assessmentVisualThemes.js
```

每个主题提供：

```js
{
  key: 'entrepreneur_spirit',
  title: '创业精神',
  motif: 'spark',
  gradient: '由 skill 自行选择',
  particles: true,
  decoration: 'energy-lines'
}
```

注意：这里不要把颜色写死，颜色由 UI skill 根据项目现有设计系统选择。

#### 方案 B：CSS 字形增强

不用图片，直接用 CSS 做高级字效：

- 渐变描边字。
- 文字阴影错位。
- 半透明玻璃浮层。
- 小标签穿插在标题周围。
- 标题后方加入淡淡的图形纹理。
- 标题不是绝对居中，可以采用左上 / 右下 / 斜向排布。

可用 CSS 技术：

```css
background-clip: text;
-webkit-text-stroke;
filter: drop-shadow();
::before / ::after;
mask-image;
radial-gradient;
linear-gradient;
```

#### 方案 C：卡片头部改成“小型数据仪表盘”

不再把标题当海报，而是把顶部做成测评画像感：

- 左侧：主题图标或抽象图形。
- 中间：测评名称 + 一句话价值说明。
- 右侧：题数、预计时长、能力标签。
- 背景：淡淡的雷达网格、曲线或粒子。
- 鼠标悬停时：图形轻微流动，按钮出现箭头动效。

这种方式更像平台级产品，不容易显得幼稚。

### 3.5 建议的卡片信息结构

每张卡片建议结构如下：

```text
┌──────────────────────────────┐
│ 顶部主题视觉区                │
│ - 主题图形                    │
│ - 艺术化标题                  │
│ - 少量动态装饰                │
├──────────────────────────────┤
│ 测评名称                      │
│ 一句话说明                    │
│ 标签：10题 / 10分钟 / 能力画像 │
│ 操作按钮：开始测评             │
└──────────────────────────────┘
```

### 3.6 卡片动效要求

动效要克制：

- 卡片 hover 上浮 4–8px。
- 顶部主题图形轻微漂浮或呼吸。
- 连接线、粒子、雷达圈可以慢速动。
- 按钮 hover 时箭头轻微右移。
- 不要使用大幅旋转、闪烁、抖动等廉价动效。

### 3.7 兼容性与性能要求

- 不要为了视觉效果引入过重依赖。
- 尽量用 SVG + CSS 完成。
- 需要适配不同屏幕宽度。
- 移动端卡片可以变成单列。
- 保证文字可读性，不要为了字效牺牲可读性。

---

## 4. PDF 报告模板重构

### 4.1 当前问题

当前 PDF 报告需要重构，不是微调。

主要问题：

1. 首页信息太少，视觉重心不稳。
2. 分数展示像孤立的大数字，缺少解释。
3. 报告标题、文件名、评估类型、生成时间等信息没有形成专业报告头部。
4. 表格区域层级弱，像数据直接堆上去。
5. 部分内容在 PDF 里表现不完整，可能存在字体、列宽、分页或渲染问题。
6. 报告整体缺少“这是一份完整专业分析报告”的感觉。

### 4.2 重构目标

新的 PDF 报告必须满足：

1. 打开第一页就像正式报告，而不是临时导出的页面截图。
2. 首页必须信息完整，不要大面积空洞。
3. PDF 中的中文必须完整显示。
4. 表格不能缺字、不能列错位、不能只有数字没有说明。
5. 不同报告类型要有不同标题和维度。
6. 下载文件必须非空。
7. 报告可以打印、转发、展示，不丢脸。

### 4.3 推荐 PDF 页面结构

#### 第 1 页：封面 + 总览页

第一页不要只放大数字。建议结构：

```text
顶部：品牌报告头
- 火花智创 SparkAI Innovate
- AI材料评估系统
- 报告类型：路演PPT评估报告 / 项目报告评估报告 / 在线测评报告

中部：总览卡片
- 总分
- 等级
- 一句话综合评价
- 文件名
- 评估类型
- 生成时间

下部：三列摘要
- 核心优势
- 主要风险
- 优先优化方向

底部：报告说明
- 本报告基于材料结构、内容完整度、表达质量、创新价值等维度生成。
```

第一页必须有足够内容，但不要堆满。

#### 第 2 页：评分维度页

```text
标题：评分维度总览

内容：
- 维度得分表
- 每项得分 / 满分
- 每项一句评估说明
- 可选：条形进度或雷达图
```

要求：

- 表头清晰。
- 每行高度足够。
- 评分说明不能被截断。
- 分数用视觉强调，但不要喧宾夺主。
- 表格需要适合 A4 宽度。

#### 第 3 页：核心评价与问题分析

```text
标题：核心评价

内容：
- 综合评价段落
- 关键问题列表
- 问题严重程度
- 对项目竞争力的影响
```

#### 第 4 页及以后：详细建议

```text
标题：详细问题分析与优化建议

分组：
- 内容结构
- 创新表达
- 市场与商业逻辑
- 团队与执行
- 风险与可行性
- 展示与说服力

每组包含：
- 优势亮点
- 当前不足
- 优化建议
- 可参考做法
```

最后一页：

```text
下一步行动
- 3–5 条优先事项
- 每条都要具体、可执行
```

### 4.4 PPT 评估报告与项目报告评估报告必须区分

#### PPT 评估报告维度建议

PPT 评估更关注路演表达：

1. 结构完整性
2. 视觉版式与阅读体验
3. 痛点表达清晰度
4. 解决方案呈现
5. 创新亮点表达
6. 商业与市场逻辑
7. 团队与执行计划
8. 路演说服力

#### 项目报告评估维度建议

项目报告更关注材料内容和论证深度：

1. 项目摘要与定位
2. 市场痛点与需求论证
3. 创新价值与技术路线
4. 商业模式与可持续性
5. 实施路径与里程碑
6. 团队能力与资源匹配
7. 财务、风险与应对
8. 材料规范与论证完整性

### 4.5 报告术语库

后端可以准备多套术语模板，避免每次报告都像同一个模板。

#### 等级文案

```text
优秀：该材料整体表现优秀，在多个维度上达到了较高水准。
良好：该材料具备较完整的表达基础，核心逻辑较为清晰。
待提升：该材料具备基础框架，但在深度、细节或表达完整性上仍有提升空间。
```

#### 核心优势术语

```text
- 项目定位较明确，能够围绕目标场景展开材料表达。
- 材料结构较完整，评审能够较快理解项目主线。
- 市场机会识别较清晰，目标用户画像具备一定基础。
- 创新亮点具备辨识度，能够体现项目差异化方向。
- 团队分工与执行计划具有一定可操作性。
```

#### 待改进术语

```text
- 创新点与竞品或现有方案的差异化对比仍不够直观。
- 痛点描述偏向通用问题，缺少针对细分场景的深度分析。
- 商业模式的收入来源、成本结构或增长路径仍偏概念化。
- 风险识别较少，缺少对实施难点和外部不确定性的应对方案。
- 材料中的数据支撑不足，需要补充调研、验证或试点反馈。
```

#### 优化建议术语

```text
- 建议补充真实用户访谈、竞品对比或试点反馈数据，用事实增强痛点可信度。
- 建议优化页面信息层级，每页聚焦一个核心观点，降低阅读负担。
- 建议为商业模式补充简要盈利测算、成本拆解或增长模型。
- 建议将创新点拆分为技术创新、模式创新、应用场景创新三个层面分别表达。
- 建议增加阶段性成果展示，用数据图表替代纯文字描述。
```

---

## 5. PDF 下载 0KB Bug 修复方案

### 5.1 现象

每次点击下载 PDF，会先下载一个 0KB 的 PDF 文件，然后又下载一个真正的 PDF 文件。

### 5.2 高概率原因

AICoder 需要重点检查以下问题：

1. 前端同时触发了两套下载逻辑。

   - 例如同时用了 `<a href="/api/xxx/download" download>` 和 JS `axios blob` 下载。
   - 或按钮外层和内层都绑定了点击事件。
2. 前端在真实 PDF Blob 返回前，就先创建了一个空 Blob。

   - 例如 `new Blob([])` 被提前下载。
   - 或接口失败时仍执行了下载逻辑。
3. 接口返回错误 JSON 或空响应，但前端仍把它当 PDF 下载。

   - 例如后端返回 500/404/401 的 JSON，前端没有判断。
   - 或异步任务未完成时返回空内容。
4. 下载函数被调用了两次。

   - 例如按钮点击事件 + 表单 submit 事件同时触发。
   - 或组件 watch / mounted 里也触发下载。
5. 后端在生成 PDF 前先返回了空文件。

   - 例如先创建临时文件，再异步写入，前端过早下载。
   - 或 `send_file` 指向了一个尚未写入完成的路径。

### 5.3 前端修复要求

下载函数必须保证：

1. 用户点击一次，只进入一个下载函数。
2. 下载中禁用按钮，避免重复点击。
3. 请求必须设置 `responseType: 'blob'`。
4. 收到响应后检查：
   - HTTP 状态码是否成功。
   - Blob size 是否大于 0。
   - Content-Type 是否为 PDF 或 octet-stream。
5. 如果 Blob 为空，不允许触发浏览器下载。
6. 只创建一次 `URL.createObjectURL(blob)`。
7. 只调用一次 `a.click()`。
8. 下载后调用 `URL.revokeObjectURL(url)`。
9. 所有错误都用通知提示，不生成空 PDF。

推荐前端伪代码：

```js
const downloading = ref(false)

async function downloadReport(reportId) {
  if (downloading.value) return

  downloading.value = true
  try {
    const response = await request.get(`/material-evaluations/${reportId}/download`, {
      responseType: 'blob'
    })

    const blob = response.data

    if (!blob || blob.size === 0) {
      ElMessage.error('PDF 文件生成失败，请稍后重试')
      return
    }

    const contentType = response.headers?.['content-type'] || ''
    if (!contentType.includes('pdf') && !contentType.includes('octet-stream')) {
      ElMessage.error('下载内容格式异常，请稍后重试')
      return
    }

    const url = window.URL.createObjectURL(blob)
    const a = document.createElement('a')
    a.href = url
    a.download = buildReportFileName()
    document.body.appendChild(a)
    a.click()
    document.body.removeChild(a)
    window.URL.revokeObjectURL(url)
  } catch (error) {
    ElMessage.error('PDF下载失败，请稍后重试')
  } finally {
    downloading.value = false
  }
}
```

同时检查模板中是否存在类似：

```html
<a href="..." download>
  <el-button @click="downloadReport">下载PDF</el-button>
</a>
```

如果有，必须删除外层 `<a>`，只保留按钮点击逻辑。

### 5.4 后端修复要求

后端下载接口必须保证：

1. PDF 生成完成后再返回。
2. 不返回空文件。
3. 不把错误 JSON 伪装成 PDF。
4. 正确设置：
   - `Content-Type: application/pdf`
   - `Content-Disposition: attachment; filename*=UTF-8''xxx.pdf`
5. 如果生成失败，返回统一 JSON 错误，不返回 0KB PDF。
6. 如果使用临时文件，必须在写入完成并确认 size > 0 后再 `send_file`。
7. 如果使用内存流，必须在 `BytesIO` 写入完成后 `seek(0)`。

后端伪代码：

```python
pdf_bytes = generate_pdf(report)

if not pdf_bytes or len(pdf_bytes) == 0:
    return error('PDF生成失败，请稍后重试', 500)

buffer = BytesIO(pdf_bytes)
buffer.seek(0)

return send_file(
    buffer,
    mimetype='application/pdf',
    as_attachment=True,
    download_name=filename
)
```

### 5.5 验收标准

1. 点击一次下载按钮，只下载一个文件。
2. 不再出现 0KB PDF。
3. 下载失败时只提示错误，不触发下载。
4. 网络失败、接口 500、未登录等场景不会生成空 PDF。
5. 连续快速点击按钮不会重复下载。
6. 控制台没有未处理异常。

---

## 6. PDF 生成技术路线建议

AICoder 需要先检查当前项目已有 PDF 生成方式，再决定是否重构。

### 6.1 推荐优先级

#### 优先级 1：后端生成 PDF，前端只负责下载

适合正式功能：

```text
前端点击下载
→ 请求后端 download 接口
→ 后端生成 PDF bytes
→ send_file 返回
→ 前端 blob 下载
```

优点：

- 稳定。
- 权限好控制。
- 结果可复现。
- 更适合以后做历史报告。

#### 优先级 2：前端 HTML 报告转 PDF

适合快速实现：

```text
前端报告页面
→ html2canvas / jsPDF
→ 下载
```

缺点：

- 中文字体、分页和清晰度容易出问题。
- 页面复杂时容易出现截断。
- 需要谨慎处理滚动区域。

#### 优先级 3：浏览器打印样式

可以作为补充：

```text
报告页提供打印版样式
用户可浏览器另存 PDF
```

不建议作为唯一方案，因为用户体验不够完整。

### 6.2 中文字体要求

PDF 中中文必须完整显示。
AICoder 应使用运行环境中已有的中文字体或浏览器渲染能力，不要把字体文件提交到仓库，也不要硬编码依赖某个不可用字体。

---

## 7. 报告页面和 PDF 模板复用

建议把“网页报告页”和“PDF 报告模板”做成同一套数据结构，不要两边各写一套数据。

数据结构建议：

```js
{
  id,
  type: 'ppt' | 'report' | 'assessment',
  title,
  score,
  level,
  fileName,
  generatedAt,
  summary,
  dimensions: [
    {
      name,
      score,
      maxScore,
      comment
    }
  ],
  strengths: [],
  weaknesses: [],
  suggestions: [],
  nextActions: []
}
```

前端网页报告页和后端 PDF 模板都消费这个结构。

这样可以避免：

- 网页显示正常，PDF 缺字段。
- PDF 维度顺序和页面不一致。
- 修改文案时两边不同步。

---

## 8. 在线测评报告也应避免低质 PDF

如果在线测评也提供报告下载，应该复用一套“报告生成基础组件 / 基础模板”。

在线测评报告可以采用不同的内容结构：

```text
第 1 页：测评结果总览
第 2 页：维度画像
第 3 页：能力解释
第 4 页：成长建议
```

不要直接复用 AI 材料评估的 PPT / 报告维度。

---
