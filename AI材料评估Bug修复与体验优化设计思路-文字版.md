# AI材料评估 Bug 修复与体验优化设计思路（纯文字版）

> 适用模块：火花智创 SparkAI Innovate / AI材料评估  
> 使用对象：AICoder  
> 重要说明：本文已经把所有页面问题、布局问题和修复目标全部改写成文字描述。AICoder 不需要、也不应该依赖任何外部视觉材料来理解需求。

---

## 1. 本次任务定位

本次不是重新开发 AI材料评估功能，而是在已实现功能基础上做一次深度 Bug 修复与体验优化。

需要围绕以下 5 类问题处理：

1. **PDF 下载失败**：PPT评估报告页和项目报告评估报告页点击“下载 PDF”后都会失败。
2. **报告页总分区域位置不合理**：总分圆环与摘要区域整体太靠近页面上方，视觉重心偏上，不够像正式报告。
3. **AI材料评估中心布局不好看**：评估类型选择区层级混乱，两个入口卡片摆放不够自然，整体像临时页面而不是正式产品功能页。
4. **上传页布局不好看且太靠上**：上传卡片位置过高，页面空白关系不自然，用户视线无法自然聚焦在上传区域。
5. **第一次上传不生效**：用户第一次选择 PDF 后没有立即进入已选择状态，通常需要再次选择同一文件或重新上传才生效。

---

## 2. 对 AICoder 的核心要求

### 2.1 不依赖外部视觉材料

AICoder 只需要阅读本文的文字要求，不要依赖任何“按外部画面修改”之类描述。

本文件已经把需要修复的现象完整文字化，AICoder 只需要按照本文描述进行排查、实现和验证。

### 2.2 先修真实 Bug，再做 UI 优化

不要只改样式。必须先定位并修复真实问题：

- PDF 下载失败到底是前端请求问题、Blob 处理问题、后端接口问题、权限问题、中文文件名问题，还是 PDF 生成问题。
- 第一次上传不生效到底是上传组件事件问题、文件状态未同步、按钮依赖状态更新滞后，还是 input value 没有重置。

UI 优化必须建立在功能链路可用的基础上。

### 2.3 用户界面不能出现模拟痕迹

前端用户界面中不要出现以下字样：

- 模拟
- 随机
- mock
- demo
- 假数据
- 占位

即使后端使用轻量评分逻辑，用户看到的也必须是正式、可信、专业的 AI材料评估流程。

### 2.4 不限制具体样式

本文只描述体验目标、布局目标和质量要求，不指定固定颜色、字号、阴影、圆角、动效参数。

AICoder 应先调用 `skill-router`，让它根据当前项目状态选择合适的 UI/UX、Debug、浏览器验证相关 skill。不要机械套用固定模板。

---

## 3. Skill 使用策略

本任务开始前必须先使用 `skill-router` 判断当前 available skills。

不要虚构 skill 名称，不要把 gstack 的内部能力当作顶层 skill 声称调用。

建议路线如下，但最终以 `skill-router` 判断结果为准：

1. **Bug 诊断阶段**  
   使用适合 Debug / Diagnose 的 skill，先排查 PDF 下载失败和第一次上传不生效的真实原因。

2. **前端页面优化阶段**  
   使用适合 frontend design / UI polish 的 skill，优化 AI材料评估中心、上传页、报告页的布局与视觉层级。

3. **完整实现阶段**  
   使用适合完整输出约束的 skill，避免只改一半、漏文件、留 TODO 或产生假按钮。

4. **浏览器验证阶段**  
   使用顶层 `gstack`，并在任务中使用它的 browser testing / QA 能力，走通完整链路：进入评估中心 → 选择评估类型 → 上传 PDF → 开始评估 → 查看评分 → 下载 PDF。

5. **测试阶段**  
   涉及接口、下载、上传状态时，可使用适合 TDD 或最小测试的 skill 做验证。

注意：本任务已全部文字化，不需要使用视觉还原方式。

---

## 4. 问题一：PDF 下载失败

### 4.1 问题表现

在评估报告页点击“下载 PDF”后，页面提示下载失败。该问题同时存在于两个报告页：

- 路演 PPT 评估报告页
- 项目报告评估报告页

必须两个都修，不能只修其中一个。

### 4.2 需要排查的前端问题

AICoder 需要检查：

1. 下载按钮是否绑定了正确的评估记录 ID。
2. PPT评估报告页和项目报告评估报告页是否使用了同一套下载逻辑，还是各自有重复实现。
3. 前端请求 URL 是否与后端实际路由一致。
4. Axios 请求是否设置了 `responseType: 'blob'`。
5. 项目封装的 `request.js` 是否会自动解包 `res.data`，导致下载逻辑取值错误。
6. Blob 构造是否正确。
7. 下载文件名是否经过安全处理。
8. 创建 `objectURL` 后是否正常触发 `<a>` 标签点击。
9. 下载结束后是否调用 `URL.revokeObjectURL`。
10. 如果后端返回 JSON 错误，前端是否误把错误 JSON 当 PDF 处理。

### 4.3 需要排查的后端问题

AICoder 需要检查：

1. 是否存在真实 PDF 下载接口。
2. 下载接口是否注册到了 Flask 蓝图中。
3. 路由前缀是否与前端请求一致。
4. 接口是否通过 JWT 鉴权。
5. 当前登录用户是否有权限下载该评估报告。
6. 后端是否真的返回 PDF 二进制，而不是 JSON。
7. 响应头是否包含：`Content-Type: application/pdf`。
8. 响应头是否包含合适的 `Content-Disposition`。
9. 中文文件名是否使用 `filename*=UTF-8''...` 形式处理。
10. PDF 生成依赖是否支持中文字体。
11. 临时目录或输出目录是否存在。
12. 生成失败时是否返回了统一错误格式。

### 4.4 推荐修复方向

优先采用后端生成 PDF、前端 Blob 下载的稳定方案。

推荐接口形式之一：

```http
GET /api/material-evaluations/<id>/pdf
```

如果项目中已经有其他命名方式，优先沿用现有路由风格，但必须保证 PPT评估和报告评估都能通过同一套可靠逻辑下载。

后端返回要求：

```http
Content-Type: application/pdf
Content-Disposition: attachment; filename*=UTF-8''AI材料评估报告.pdf
```

前端下载逻辑要求：

```js
const response = await request.get(downloadUrl, {
  responseType: 'blob'
})

const blobData = response instanceof Blob ? response : response.data
const blob = new Blob([blobData], { type: 'application/pdf' })
const objectUrl = URL.createObjectURL(blob)

const link = document.createElement('a')
link.href = objectUrl
link.download = filename
link.style.display = 'none'
document.body.appendChild(link)
link.click()
link.remove()
URL.revokeObjectURL(objectUrl)
```

注意：上面的代码只是方向，最终要根据项目 `request.js` 的实际返回结构调整。

### 4.5 下载按钮交互要求

下载按钮需要具备状态反馈：

- 默认：下载 PDF
- 请求中：下载中，按钮禁用
- 成功：提示“PDF 已开始下载”或不打扰式轻提示
- 失败：提示“PDF下载失败，请稍后重试”

下载失败时，控制台要保留开发者可读的错误信息，但不要把内部错误堆栈暴露给普通用户。

---

## 5. 问题二：报告页总分区域太靠上

### 5.1 问题表现

报告页顶部标题区域结束后，总分圆环和摘要内容出现得过早，整体贴近页面上方，导致视觉上不够舒展，也不像一份完整的正式报告。

### 5.2 优化目标

报告页需要形成稳定的阅读层级：

```text
顶部导航
↓
报告标题区
↓
报告摘要总览卡片
↓
评分维度卡片
↓
评分明细 / 核心评价 / 优势亮点 / 问题分析 / 优化建议
↓
下载 PDF / 返回按钮
```

### 5.3 摘要总览卡片要求

总分圆环不要孤立显示，也不要贴在标题区下沿。

建议把总分圆环、等级标签、综合评价、文件名、评估类型、生成时间放进同一个“报告摘要总览卡片”中。

摘要总览卡片内部可以采用左右结构：

```text
左侧：总分圆环 + 等级标签
右侧：综合结论 + 文件信息 + 评估类型 + 生成时间
```

也可以采用更适合当前项目的结构，但必须满足：

1. 总分区域不贴顶。
2. 摘要信息完整。
3. 视觉重心在页面中上部，而不是紧贴顶部。
4. PPT评估报告页和项目报告评估报告页都要统一优化。

---

## 6. 问题三：AI材料评估中心布局不好看

### 6.1 问题表现

AI材料评估中心的“选择评估类型”区域看起来不够正式，两个评估入口卡片的布局、间距、信息层级不自然，像临时拼出来的页面。

### 6.2 优化目标

AI材料评估中心应该像一个正式产品功能页，用户一进入就能清楚知道：

1. 这个功能是做什么的。
2. 支持哪两类评估。
3. PPT评估和报告评估互不绑定，可以单独使用。
4. 每类评估需要上传什么材料。
5. 每类评估会输出什么结果。
6. 下一步应该点击哪个按钮。

### 6.3 推荐信息结构

建议结构：

```text
页面标题：AI材料评估中心
功能说明：上传 PDF 材料，获取路演 PPT 表现力与项目报告质量的智能评分建议
格式说明：仅支持 PDF 文件，请先将 PPT、商业计划书、项目申报书或项目报告导出为 PDF 后上传
↓
模块标题：选择评估类型
模块说明：PPT评估与报告评估互不依赖，可独立使用
↓
两个并列入口卡片：
  入口一：PPT评估
  入口二：报告评估
↓
可选说明区：评估流程 / 数据安全 / 支持格式
```

### 6.4 PPT评估卡片内容要求

PPT评估入口卡片要表达清楚：

- 适用材料：路演 PPT 导出的 PDF。
- 关注方向：结构完整性、视觉表达、逻辑叙事、创新亮点、商业呈现、路演说服力。
- 输出结果：总分、维度评分、核心评价、优势亮点、主要问题、优化建议。
- 按钮文案：开始 PPT 评估。

### 6.5 报告评估卡片内容要求

报告评估入口卡片要表达清楚：

- 适用材料：商业计划书、项目申报书、项目报告导出的 PDF。
- 关注方向：内容完整度、市场论证、创新价值、实施路径、风险应对、材料规范性。
- 输出结果：总分、维度评分、核心评价、优势亮点、主要问题、优化建议。
- 按钮文案：开始报告评估。

### 6.6 布局质量要求

不指定具体风格，但必须满足：

1. 页面标题、说明、卡片之间有明确层级。
2. 两个评估入口视觉上平级。
3. 卡片不要过宽、过窄或孤立漂浮。
4. 按钮要足够明显。
5. 桌面端建议横向并列，窄屏自然纵向排列。
6. 不要出现大面积无意义空白。
7. 不要让“选择评估类型”贴在顶部标题区下方。

---

## 7. 问题四：上传页布局太靠上

### 7.1 问题表现

上传页整体内容位置偏上，上传卡片没有位于自然视觉中心。页面看起来上方拥挤，下方空旷，用户体验不舒服。

### 7.2 优化目标

上传页应该像正式的材料提交页，包含清晰状态：

1. 当前流程标题。
2. 返回评估中心入口。
3. 文件格式要求。
4. 上传卡片。
5. 文件已选择状态。
6. 上传中状态。
7. 上传失败状态。
8. 开始 AI 评估按钮。

### 7.3 推荐结构

```text
顶部区域：返回评估中心 + 当前流程标题
↓
说明区域：仅支持 PDF，请先将材料导出为 PDF 后上传
↓
上传卡片：
  未选择文件：上传图标 + 点击或拖拽 PDF 到此处
  已选择文件：文件名、文件大小、移除按钮
  上传中：进度条或 loading 状态
  上传失败：错误提示 + 重新选择
↓
操作按钮：返回 / 开始 AI 评估
```

### 7.4 布局质量要求

1. 上传卡片不要靠近页面顶部。
2. 上传卡片应是页面主要视觉焦点。
3. 按钮区域不要离上传卡片过远。
4. 页面整体应有呼吸感，但不能显得空。
5. PPT上传页和报告上传页共用结构，但标题和说明文案不同。

---

## 8. 问题五：第一次上传不生效

### 8.1 问题表现

用户第一次选择 PDF 文件后，页面没有立即显示文件已选择，也没有立即启用“开始 AI 评估”按钮。用户通常需要再次选择文件，页面状态才正常。

### 8.2 常见原因

AICoder 需要重点排查：

1. 使用 Element Plus Upload 时，`before-upload` 返回了 `false`，但没有在 `on-change` 中保存 `raw` 文件。
2. 文件进入了组件内部队列，但没有同步到业务状态 `selectedFile`。
3. `fileList` 和自定义文件状态不一致。
4. 同一个文件重复选择时，原生 input 的 value 没有清空，导致 change 事件不触发。
5. “开始 AI 评估”按钮依赖的状态更新滞后。
6. 上传按钮点击只改变了 UI 状态，没有真正触发文件选择。
7. PDF 校验逻辑过早 return，导致状态没有设置。

### 8.3 推荐修复方式

采用“手动上传 + 文件状态立即保存”的模式。

选择文件后应立即执行：

1. 获取 `raw` 文件。
2. 校验文件类型是否为 PDF。
3. 校验通过后保存到 `selectedFile`。
4. 同步更新 `fileList`。
5. 页面立即显示文件名和大小。
6. 立即启用“开始 AI 评估”按钮。
7. 移除文件时同步清空 `selectedFile` 和 `fileList`。

参考逻辑：

```js
const selectedFile = ref(null)
const fileList = ref([])

const handleFileChange = (uploadFile) => {
  const raw = uploadFile.raw
  if (!raw) return

  const isPdf = raw.type === 'application/pdf' || raw.name.toLowerCase().endsWith('.pdf')
  if (!isPdf) {
    ElMessage.error('仅支持上传 PDF 文件')
    selectedFile.value = null
    fileList.value = []
    return
  }

  selectedFile.value = raw
  fileList.value = [uploadFile]
}

const handleRemove = () => {
  selectedFile.value = null
  fileList.value = []
}
```

如果使用原生 input，需要在每次打开文件选择器前清空 value：

```js
input.value = ''
input.click()
```

### 8.4 验证标准

用户第一次选择 PDF 后，页面必须立即显示：

- 文件名
- 文件大小
- 移除按钮
- 可点击的“开始 AI 评估”按钮

不允许再出现需要重复选择同一文件才生效的情况。

---

## 9. PPT评估与报告评估都要同时修复

不要只修一个流程。

必须检查并修复：

1. PPT评估上传页。
2. 报告评估上传页。
3. PPT评估报告页。
4. 报告评估报告页。
5. 两个报告页的 PDF 下载。
6. 两个报告页的总分摘要区域布局。
7. 两个流程的返回路径。
8. 两个流程的错误提示。

PPT评估和报告评估互不绑定，不能要求用户两个都上传。

---

## 10. 报告页内容结构要求

报告页至少包含：

1. 报告标题。
2. 下载 PDF 按钮。
3. 总分。
4. 等级标签。
5. 综合结论。
6. 文件名。
7. 评估类型。
8. 生成时间。
9. 评分维度。
10. 评分明细。
11. 核心评价。
12. 优势亮点。
13. 主要问题。
14. 优化建议。
15. 下一步行动建议。

报告页需要有完整感，不要只有分数和几条短文案。

---

## 11. PDF 报告内容要求

下载出来的 PDF 应该包含当前页面报告的核心信息，不能只是空白 PDF 或只有标题。

PDF 至少包含：

1. 报告名称。
2. 评估类型。
3. 文件名。
4. 总分。
5. 等级。
6. 评分维度表。
7. 综合评价。
8. 优势亮点。
9. 主要问题。
10. 优化建议。
11. 生成时间。

中文内容不能乱码。

如果后端生成 PDF，注意中文字体。若当前环境缺少中文字体，需要使用项目可用字体或系统字体路径，并在代码中做兜底处理。

---

## 12. 路由与接口建议

如果现有路由已经实现，优先修复现有路由，不要为了重构而大改。

推荐页面路由结构：

```text
/material-evaluation                  AI材料评估中心
/material-evaluation/upload/:type      上传页，type = ppt/report
/material-evaluation/analyzing/:id     分析进度页
/material-evaluation/report/:id        报告页
```

推荐后端接口结构：

```http
POST /api/material-evaluations
GET  /api/material-evaluations/<id>
GET  /api/material-evaluations/<id>/pdf
```

接口命名可以根据项目现状调整，但必须保证：

1. 前后端路径一致。
2. 鉴权一致。
3. 返回格式一致。
4. 错误处理一致。
5. PDF 下载接口真实可用。

---

## 13. 错误提示要求

### 13.1 上传错误

非 PDF 文件：

```text
仅支持上传 PDF 文件，请先将材料导出为 PDF 后再上传
```

文件过大：

```text
文件过大，请压缩后重新上传
```

上传失败：

```text
文件上传失败，请稍后重试
```

### 13.2 下载错误

PDF 下载失败：

```text
PDF下载失败，请稍后重试
```

权限不足：

```text
暂无权限下载该报告
```

报告不存在：

```text
报告不存在或已被删除
```

---



